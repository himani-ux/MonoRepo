**What & why** - Add the missing Audit master-data API surface identified in the 2026-08-18 resolution: qualified auditors, external audit organisations, and vessel RO delegations. These APIs allow approved Audit operators to manage operational master data without changing the existing database schema or adding frontend screens.

**Tier + triggers** - Tier 2. Triggers: new backend endpoints, changed Audit permission catalog, and new role-gated behavior.

**Domains touched** - Audit master data, Audit RBAC, Audit backend API contracts.

**Decisions** - D-MAINT-CR148: use the existing `APIView` and `HasAuditProcessPermission` patterns. Add `AUDIT_P_019` for external audit organisation management and `AUDIT_P_020` for vessel RO delegation management. Keep `AUDIT_P_009` for qualified auditor management. Use the existing model fields and common user/crew references; do not add migrations or duplicate identity data.

**Doc cascade** - Updated `audit_docsuite/BACKEND_STRUCTURE.md` with the three master API contracts. Updated `audit_docsuite/GAPS_2.md` to record the implementation status. `audit_docsuite/RBAC.md` and the frozen SSOT are not present in this workspace; no substitute permission authority was invented.

**Tests** - Added route, serializer, process-gate catalog, active-organisation validation, and overlapping RO delegation validation tests. Hard delete was not implemented because the resolution specifies deactivation/effective-date handling for operational master data.

**Fidelity check** - No frontend screens or database migration were added; the qualified-auditor route uses `AUDIT_P_009`, and the two new routes use `AUDIT_P_019`/`AUDIT_P_020` as approved by the implementation request. Existing office-user and vessel-crew identity sources remain unchanged.

**Exemptions** - none.
