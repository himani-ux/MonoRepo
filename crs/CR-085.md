**What & why** - The Safety sidebar currently exposes a broad **Admin** entry even though the useful user-facing admin shortcut is Auditor Export. This change removes the **Admin** item from the sidebar while keeping **Auditor Export** visible for users with `SAF_F_020`.

**Tier + triggers** - Tier 2. Triggers: changed visible navigation state/sidebar behavior.

**Domains touched** - Step 1 domains: Safety sidebar navigation, Auditor Bundle Export navigation, frontend sidebar tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR085: the current Safety sidebar does not show a broad **Admin** navigation item. **Auditor Export** remains a direct sidebar link gated by `SAF_F_020`. Existing admin routes remain permission-gated and are not advertised in the sidebar.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`, `safety_docsuite/APP_FLOW.md`, and `safety_docsuite/USER_GUIDE.md` describe the current sidebar behavior. `safety_docsuite/BACKEND_STRUCTURE.md` unchanged because no backend route, permission, or payload changes. `safety_docsuite/PRD.md` unchanged because the auditor export feature contract remains unchanged. `Docs/progress.txt` records the change.

**Tests** - Update `psc-frontend/tests/frontend/safety/routes.test.tsx` to prove **Admin** is absent from the sidebar while **Auditor Export** remains visible when permitted.

**Fidelity check** - no deltas. The sidebar removes the broad Admin entry and keeps the useful Auditor Export link as requested.

**Exemptions** - none.
