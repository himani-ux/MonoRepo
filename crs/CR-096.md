**What & why**

Added a focused Certs approval queue so Master can find certificate uploads waiting for approval/rejection instead of hunting through vessel certificate rows. The vessel status filter also now includes `Pending master approval`, which filters by `approval_state` rather than certificate expiry status.

**Tier + triggers**

Tier 2 - small feature/behavior change. Triggers: new screen state/route, changed API query contract, and changed role-gated Certs UI.

**Domains touched**

Step 1 domains: Certs tracked-item approval workflow, Certs frontend navigation/filtering, Certs API list filtering, and RBAC visibility.

**Decisions**

No new SSOT decision. Accepted default: the queue is visible to Master and office-side users with tracked-item form access; actual approve/reject actions remain aligned with the existing Master approval workflow.

**Doc cascade**

- `certs_docsuite/APP_FLOW.md` - documented `/certs/approvals` in navigation, screen catalog, and role matrix.
- `certs_docsuite/USER_GUIDE.md` - added simple instructions for Master/office approval queue use.

**Tests**

Added repository/API contract coverage for `approval_state` filtering and ran focused compile/type checks where possible.

**Fidelity check**

No deltas. The requested status option and separate approval/rejection window are present, and the queue is restricted to Master/office visibility.

**Exemptions**

none
