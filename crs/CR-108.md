**What & why** - Generated Certs print artifacts must be directly usable. The current screens only show PDF/Excel/ZIP readiness, and recipient email is stored without sending anything. This change will add authenticated artifact download access and send generated print/share files to the recipient email when one is provided, using the existing platform/Circular email configuration instead of separate Certs-specific mail settings.

**Tier + triggers** - Tier 3. Triggers: new artifact download endpoint/contract, new email dispatch behavior for Certs print/share artifacts, and reuse of existing cross-module/platform email infrastructure.

**Domains touched** - Certs print/export workflow, Certs share bundle workflow, print artifact storage/download, email dispatch, frontend artifact result UI, backend tests, user-facing Certs documentation.

**Decisions** - D-CERT-201 added in `certs_docsuite/IMPLEMENTATION_PLAN.md` Amendment 4: successful print/share generation must provide direct authenticated artifact access, and non-empty recipient email triggers delivery through the existing platform email configuration. Accepted defaults: keep print/share generation synchronous as today; show download actions for whichever artifacts were generated; email only when recipient email is entered; send PDF+Excel for Print Builder and ZIP for Share Bundle; use existing email backend/settings already used by Circular/platform mail; record email-send result in print artifact audit metadata without changing the print artifact table schema.

**Doc cascade** - `certs_docsuite/IMPLEMENTATION_PLAN.md` amended for D-CERT-201. `certs_docsuite/APP_FLOW.md` now documents authenticated download buttons and email sent/failed status. `certs_docsuite/BACKEND_STRUCTURE.md` now documents `print_delivery.py`, the download endpoint, and existing SMTP reuse. `certs_docsuite/FIELD_MAP.md` now maps `downloadUrls`, email status response fields, and endpoint-backed blob access. `certs_docsuite/USER_GUIDE.md` now tells users to download generated files and explains recipient email behavior. `Docs/progress.txt` records this change.

**Tests** - Added backend regression coverage for print artifact download route registration, serializer download URLs, view-level recipient email dispatch, PDF+Excel email attachments, ZIP-only share-bundle email attachments, and authenticated download streaming. Added frontend route validation for download buttons and email status on print/share results.

**Fidelity check** - no deltas

**Exemptions** - none
