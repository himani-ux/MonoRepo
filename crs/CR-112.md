**What & why** - The vessel certificate list action column is being simplified so users see compact icon actions instead of a text Upload button. Each row will show an upload/open-certificate action plus a separate view-PDF action that is enabled only when that certificate already has a PDF file.

**Tier + triggers** - Tier 2. Trigger: changed screen state in the Certs vessel dashboard action column because a new disabled/enabled PDF view icon is visible per certificate row.

**Domains touched** - Step 1 Certs vessel dashboard, certificate file handling, and user-facing UI wording.

**Decisions** - Accepted default: use existing certificate detail route for upload/open behavior and existing authenticated PDF view API for the view icon. No new decision ID required.

**Doc cascade** - `certs_docsuite/APP_FLOW.md` updated to describe the two icon actions in the vessel dashboard action column. `certs_docsuite/USER_GUIDE.md` updated to explain the upload and view-PDF icons in user language. `certs_docsuite/BACKEND_STRUCTURE.md` unchanged because no endpoint or backend contract changes.

**Tests** - Frontend certs route validation test updated to cover the icon-only action column and disabled view state.

**Fidelity check** - no deltas.

**Exemptions** - none.
