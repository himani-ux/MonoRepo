**What & why** - Audit users should not type comma-separated standard codes in editable Audit forms. The form will show selectable standard options while still saving the same CSV value expected by the backend.

**Tier + triggers** - Tier 2. Trigger: changed screen behavior and form state for Audit standards selection.

**Domains touched** - Audit plan register.

**Decisions** - D-MAINT-CR147: keep the backend `audit_standards_csv` contract unchanged and convert only the user-facing plan form input into a selectable standards control.

**Doc cascade** - Updated `audit_docsuite/APP_FLOW.md` and `audit_docsuite/USER_GUIDE.md` to state that Audit plan standards are selected from options. `audit_docsuite/BACKEND_STRUCTURE.md` unchanged because API and database storage remain `audit_standards_csv`.

**Tests** - Updated `psc-frontend/src/routes/audit/plans/index.test.tsx` to exercise the standards dropdown and verify the same CSV payload is sent. Ran `npm test -- --run src/routes/audit/plans/index.test.tsx` and `npm run build`.

**Fidelity check** - no deltas. The UI no longer asks users to type standard codes, and the backend contract remains unchanged as decided.

**Exemptions** - none.
