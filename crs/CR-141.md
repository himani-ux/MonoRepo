**What & why** - The Safety Dashboard top summary repeats open counts that are already shown in the Open work pie chart, and the primary Safety score number is not clear for users. This change keeps the pie chart as the open-work view, changes the top summary boxes to total counts for the selected period, and replaces the primary score number with a plain overall status.

**Tier + triggers** - Tier 2. Triggers: changed dashboard screen presentation and additive dashboard response fields for total counts. No database, permission, dependency, or workflow change.

**Domains touched** - Step 1 domains: Safety Dashboard presentation, dashboard composite response metrics, frontend route tests, backend composite score tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR141: `/safety/dashboard/` uses open counts only for the Open work pie chart. The top summary boxes show total incidents, total near misses, total findings, total corrective actions, and SOI Compliance % for the selected period/scope. The first card shows overall status instead of the numeric score. Existing open-count fields and backend score calculation remain available for compatibility.

**Doc cascade** - Updated `safety_docsuite/PRD.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, and `safety_docsuite/VALIDATION_RULES.md` to describe overall status, total-count top boxes, and open-work pie behavior. Updated `safety_docsuite/IMPLEMENTATION_PLAN.md` with Amendment 44 and `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` with D-MAINT-CR141. Backend structure docs are unchanged because no table/schema change is required and the response change is additive.

**Tests** - Updated backend composite score tests for total metric fields and frontend dashboard route tests for overall status and total summary wording. Passed: `python manage.py test tests.safety.test_composite_score.CompositeScoreServiceTests.test_build_rollup_returns_core_counts_and_composite_score -v 2`; `npm run test -- src/routes/safety/index.test.tsx -t dashboard`; `npm run type-check`; `npm run build`.

**Fidelity check** - no deltas.

**Exemptions** - none.
