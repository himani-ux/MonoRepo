**What & why**

Incident PDF output is being adjusted so closure reason is not printed inside the Summary section. It should appear near the end of the report, immediately before the Signature section. Lessons Learned layout is also being cleaned so the lesson description is printed once inside the lesson box instead of duplicated inside and above/outside the box.

**Tier + triggers**

Tier 2 - small behavior/output contract change. Trigger: changed PDF layout contract for incident export.

**Domains touched**

Step 1 domains touched: Safety incident PDF export, final record/closure presentation, Lessons Learned presentation.

Mini-interrogation:
- Should any database column or API payload change? No. Existing `closure_reason` and lesson fields remain unchanged.
- Should closure reason be removed from the PDF entirely? No. It moves from Summary to the final area before Signature.
- Should Lessons Learned content change? No. Only duplicate rendering is removed; the saved description still prints once.
- Should section selection behavior change? No. Closure reason is tied to the Signature section and prints before Signature when that section is included.

**Decisions**

No new SSOT decision ID required; this refines PDF presentation only.

**Doc cascade**

- `safety_docsuite/APP_FLOW.md` - Incident PDF route contract now states closure reason prints before Signature and Lessons Learned descriptions are not duplicated.
- `safety_docsuite/PRD.md` - FEAT-SAF-PDF-001 acceptance criteria now include closure placement and single-render Lessons Learned descriptions.
- `safety_docsuite/USER_GUIDE.md` - User PDF guidance now says closure reason appears before Signature and lessons print once inside their box.
- `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` - Added D-MAINT-CR027 for the current incident PDF closure/Lessons layout.
- `safety_docsuite/BACKEND_STRUCTURE.md` unchanged because no schema, model, or API contract changed.
- `safety_docsuite/VALIDATION_RULES.md` unchanged because validation rules did not change.

**Tests**

- Added regression assertions in `psc-backend/tests/safety/test_pdf_10_sections.py` proving closure reason is after Lessons Learned and before Signature, and the lesson description appears only once.
- Ran `python -m unittest tests.safety.test_pdf_10_sections -v`.
- Ran `python -m unittest tests.safety.test_pdf_signature_block_by_band tests.safety.test_pdf_end_to_end -v`.

**Fidelity check**

- Mini-interrogation said no DB/API change; implementation changed only PDF rendering/template code and tests/docs.
- Mini-interrogation said closure reason must move out of Summary and remain in the PDF before Signature; test verifies the extracted PDF text order.
- Mini-interrogation said Lessons Learned content should not change, only duplicate rendering should be removed; test verifies the saved description appears once.
- No deltas.

**Exemptions**

None.
