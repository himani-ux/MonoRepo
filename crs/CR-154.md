**What & why** - Add colour-coded priority badges to the Near Miss register so users can visually distinguish HIGH, MEDIUM, LOW, and pending-priority rows in the table.

**Tier + triggers** - Tier 2. Trigger: changed frontend screen state on the existing Near Miss register.

**Domains touched** - Step 1 Safety Near Miss register/listing domain; Step 1 Safety UI status/priority display domain.

**Decisions** - No new product decision. The change only changes list presentation: HIGH is red, MEDIUM is amber, LOW is green, and missing priority remains "Pending office comments" with a neutral badge. Existing priority values, backend workflow, filters, and permissions are unchanged.

**Doc cascade** - Update `safety_docsuite/APP_FLOW.md` to document colour-coded Near Miss priority badges on the list. Update `safety_docsuite/USER_GUIDE.md` so users know the Near Miss register priority badge is colour-coded. `Docs/progress.txt` receives the append-only Maintain entry. Expected docs unchanged: backend/API docs because no data contract or endpoint changed.

**Tests** - Added focused frontend regression coverage for the Near Miss priority badge colour classes. Passed `npm test -- src/routes/safety/index.test.tsx`. Passed `npm run type-check`.

**Fidelity check** - no deltas.

**Exemptions** - none.
