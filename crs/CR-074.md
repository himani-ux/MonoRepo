**What & why** - Incident Office Review rework should remain a single free-text instruction box, with no target-phase selector. When an incident is sent back for rework, users need to see the typed rework summary on the Incident Office Review screen so they know exactly what changes the office requested.

**Tier + triggers** - Tier 2. Triggers: changed Incident Office Review screen state and changed Phase 7 preflight response contract by adding latest rework summary metadata.

**Domains touched** - Domain 2 Incident Office Review screen state, Domain 4 incident workflow/rework behavior, Domain 7 frontend/backend API contract, Domain 9 regression tests.

**Decisions** - D-MAINT-CR074 accepted user direction: Incident Office Review keeps one send-for-rework textbox only; the office-typed rework text is stored in the existing incident phase log and exposed back as the latest rework summary when the incident is in rework.

**Doc cascade** - Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` to add D-MAINT-CR074 and document the Office Review `rework_summary` contract. Updated `safety_docsuite/APP_FLOW.md` for the Phase 6 Office Review visible behavior. Updated `safety_docsuite/PRD.md` for FEAT-SAF-INC-030 acceptance criteria. Updated `safety_docsuite/USER_GUIDE.md` for how users read the rework summary. Updated `safety_docsuite/VALIDATION_RULES.md` for the no-picker send-back validation/display rule. Updated `safety_docsuite/BACKEND_STRUCTURE.md` for the phase-log storage and preflight response shape. Updated `LESSONS.md` because the user corrected the initial proposed target-picker approach. Expected docs not updated: `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because this is not a structural workflow or migration change.

**Tests** - Updated `psc-backend/tests/safety/test_phase7_sendback.py` to assert the sent-back incident preflight exposes the latest rework summary and to align the existing resubmit/accept regression with current Office Review closure at backend phase 9. Updated `psc-frontend/src/components/safety/incident/phase-7-workspace.test.tsx` to assert ship-side users can see the Rework summary and still do not see a send-back target selector.

**Fidelity check** - no deltas. The user explicitly rejected a selectable rework target and requested one textbox plus a visible summary; implementation keeps the fixed action-rework target, stores the typed text in the existing phase log, and displays that text as Rework summary while the incident is sent back.

**Exemptions** - none.
