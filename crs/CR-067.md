**What & why** - Incident PDFs should still show saved corrective/preventive due dates, but the due date must not be rendered as a separate detail row. It should stay inside the same Description cell for that action so the action block remains compact and does not create a separate left-column Due date label.

**Tier + triggers** - Tier 2; the generated Incident PDF contract changes.

**Domains touched** - Safety incident PDF generation, corrective/preventive action compatibility output, and safety documentation.

**Decisions** - Added `D-MAINT-CR067` as an append-only SSOT decision. It supersedes `D-MAINT-CR066` only for due-date placement: due dates remain visible in the PDF, but inside the Description cell rather than as a separate row.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` added `D-MAINT-CR067` and updated current PDF action-output wording. `safety_docsuite/APP_FLOW.md`, `safety_docsuite/PRD.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, and `safety_docsuite/BACKEND_STRUCTURE.md` now describe due dates as inline Description content rather than separate PDF rows. `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because no structural amendment was required and the existing implementation notes remain historical/superseded by the maintain decisions.

**Tests** - Updated `tests.safety.test_pdf_10_sections` so action blocks must contain only Description rows with the linked due date appended inside the value.

**Fidelity check** - no deltas. Mini-interrogation findings match implementation: corrective/preventive due dates remain visible in the PDF, but the row labels remain only Description and the due date appears within that Description value.

**Exemptions** - None.
