**What & why** - Phase 3 Corrective Action and Phase 4 Preventive Action must keep a Due Date field while still hiding status, closure, owner/checker, verification, risk-reduction, and other action-tracking controls. The previous simplification removed too much: it correctly removed status/closing behavior, but it also removed due-date entry and made saved due dates invisible on the action cards.

**Tier + triggers** - Tier 2. Triggers: changed visible screen state on the incident corrective/preventive action forms and corrected frontend payload behavior for the existing recommendation-linked action contract.

**Domains touched** - Step 1 domains: incident action capture, recommendation-linked corrective-action persistence, PDF/action-card source data, frontend validation/display, and maintain-mode documentation.

**Decisions** - D-MAINT-CR081: current Phase 3 Corrective Action and Phase 4 Preventive Action collect Description and optional Due Date only. Due Date is stored through the existing linked corrective-action payload and displayed as `Due Date: YYYY-MM-DD` on saved action cards. Status/open-close, verification, owner/checker, risk-reduction, theme, effort, and remaining-risk fields remain hidden from these incident action phases.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` updates the Phase 3/4 field contract and records D-MAINT-CR081. `safety_docsuite/VALIDATION_RULES.md`, `safety_docsuite/USER_GUIDE.md`, and `safety_docsuite/APP_FLOW.md` describe the restored optional Due Date field and the hidden status/closure fields. `LESSONS.md` records the user correction that Due Date is recording data, not a status/closure control. `Docs/progress.txt` records the change. Expected docs not updated: `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because this restores a field within the current simplified action capture and does not change the phase structure; `safety_docsuite/BACKEND_STRUCTURE.md` unchanged because the backend recommendation-linked corrective-action due-date contract already existed.

**Tests** - Updated frontend regression coverage in `psc-frontend/src/components/safety/incident/phase-6-workspace.test.tsx` so corrective/preventive forms show Due Date, save it through `corrective_action.due_date`, and keep hidden action-tracking fields absent. Existing backend tests already cover recommendation due-date persistence through linked corrective actions.

**Fidelity check** - no deltas. The user correction is applied as stated: Due Date is restored for Phase 3 and Phase 4 while status/closure behavior remains removed.

**Exemptions** - none.
