**What & why** - Remove the Office Review approval gate from Incident Loss Evaluation and make the Phase 4 preventive risk-reduction answer common to the screen instead of repeated on each preventive action. Users need ship-side and office-side access to fill Loss Evaluation without waiting for office approval, and the risk-reduction answer should be entered once even when multiple preventive actions are added.

**Tier + triggers** - Tier 2. Triggers: changed role/phase validation rule, changed Incident Phase 7 screen availability, changed Incident Phase 4 screen state, and regression tests for the changed behavior.

**Domains touched** - Domain 2 screen state and role-specific access, Domain 4 phase validation/workflow, Domain 7 API payload/response contract, Domain 9 regression tests.

**Decisions** - D-MAINT-CR052 accepted default: Incident Loss Evaluation is fillable by authorized ship-side and office-side users without requiring Office Review approval/current backend phase 8; Phase 4 displays one common "How much will this reduce risk?" control for all preventive actions on the screen.

**Doc cascade** - Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` to add D-MAINT-CR052, document ungated Loss Evaluation saves, and make Phase 4 risk reduction a shared screen-level answer. Updated `safety_docsuite/APP_FLOW.md` to describe the current Phase 4 and Phase 7 screen behavior. Updated `safety_docsuite/PRD.md` to revise Preventive Action and Loss Evaluation acceptance criteria. Updated `safety_docsuite/USER_GUIDE.md` to explain ship/office Loss Evaluation saves and the shared preventive risk answer. Updated `safety_docsuite/VALIDATION_RULES.md` to remove the old Office Review approval gate for Loss Evaluation saves and add the shared preventive risk-reduction rule. Updated `safety_docsuite/BACKEND_STRUCTURE.md` to document no migration, the Phase 7 GET/PATCH access rule, and the compatibility storage of shared preventive risk reduction. Updated `Docs/progress.txt` with the CR-052 ledger entry. `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because the visible Phase 7 feature already exists and this change only relaxes its save gate. `Docs/LESSONS.md` unchanged because no revert, misclassification, or lesson-triggering recurrence occurred.

**Tests** - Added backend regression in `psc-backend/tests/safety/test_phase8_loopback.py` proving a ship-side scoped user can read and save Loss Evaluation at backend `current_phase=7`. Updated frontend regressions in `psc-frontend/src/components/safety/incident/phase-8-workspace.test.tsx` for loading Loss Evaluation before Office Review approval and `psc-frontend/src/components/safety/incident/phase-6-workspace.test.tsx` for one shared preventive risk-reduction control with no repeated saved-card risk lines. Ran `python -m unittest tests.safety.test_phase8_loopback -v` and `npm.cmd test -- --run src/components/safety/incident/phase-8-workspace.test.tsx src/components/safety/incident/phase-6-workspace.test.tsx`.

**Fidelity check** - no deltas. The accepted defaults are present in backend access checks, frontend screen behavior, tests, SSOT decision D-MAINT-CR052, validation rules, PRD acceptance criteria, user guide instructions, and backend API documentation.

**Exemptions** - none.
