**What & why** - Implement the final 2026-08-20 Audit resolution rulings for the remaining build gaps: assign Lead Auditor from approved qualified-auditor data at plan level, carry the selected Lead Auditor into internal audit registration as server-side snapshots, default external audit organisation from vessel RO delegation while keeping manual override, and expose the Acting HoD coverage screen. This removes UUID/free-text workflow gaps called out by the resolution package and makes the UAT journeys executable with approved test data.

**Tier + triggers** - Tier 3; new database column on `master_audit_plan`, changed Audit plan/registration API contracts, new screen/route, changed validation/defaulting behavior, and Audit permission-gated workflow behavior.

**Domains touched** - Domain 13 Audit plan/register, Audit master data, Audit external registration, Audit Acting HoD coverage, Audit UAT readiness.

**Decisions** - D-MAINT-CR151: implement registered `D-AUDRS-139` by adding plan-level `lead_auditor_user_id` and snapshotting it into `audit_detail` at registration. Implement registered `D-AUDRS-138` usage by relying on the existing `AUDIT_P_009`, `AUDIT_P_019`, and `AUDIT_P_020` master endpoints. Implement D-212 external organisation defaulting from `vessel_audit_ro_delegation` with manual override. Implement D-253 Acting HoD coverage at `/admin/hod-coverage`.

**Doc cascade** - `Docs/IMPLEMENTATION_PLAN.md` amended with the registered resolution rulings. `Docs/progress.txt` records the shipped implementation. Existing frozen Audit SSOT/docsuite source files are not edited because the resolution states v6 reissue is owned separately by the document owner.

**Tests** - Updated backend tests for Lead Auditor plan assignment, registration snapshot behavior, external RO defaulting/manual fallback, and Acting HoD route/serializer behavior. Ran frontend production build to verify the new dropdowns and Acting HoD route compile.

**Fidelity check** - Resolution requirements implemented for plan-level Lead Auditor storage, internal registration snapshots, external organisation dropdown/defaulting, Audit master APIs, and `/admin/hod-coverage`. Remaining operational dependency: approved real master data must exist in `master_audit_qualified_auditor`, `master_external_audit_org`, and `vessel_audit_ro_delegation` before users can complete production workflows.

**Exemptions** - none.
