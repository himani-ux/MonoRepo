**What & why**

The Phase 1 Incident Report header no longer displays the right-side status badges showing the internal incident UUID and auto-save state. Users found this technical information unnecessary on the reporting screen. Auto-save behavior remains unchanged; only the visible header badges are removed.

**Tier + triggers**

Tier 2 - small behavior/UI change. Trigger: changed screen state on the Phase 1 Incident Report header.

**Domains touched**

Step 1 domains touched: Safety incident Phase 1 UI and user-facing auto-save presentation.

Mini-interrogation:
- Should auto-save be disabled? No. Draft persistence remains active; only the header status badge is removed.
- Should the incident UUID still be visible in the Phase 1 header? No. The header should show only the report title and helper text.
- Should navigation/workflow change? No. Phase cards, form save, submit, and autosave behavior are unchanged.

**Decisions**

No new SSOT decision ID required; this refines Phase 1 presentation without changing data, workflow, or permissions.

**Doc cascade**

- `safety_docsuite/APP_FLOW.md` - FEAT-SAF-AUDIT-006 now documents autosave draft persistence while keeping the Phase 1 header free of internal incident-id and auto-save badges.
- Other canonical docs unchanged because autosave behavior, persistence, workflow, data model, and permissions are unchanged.

**Tests**

- Added `psc-frontend/src/components/safety/incident/phase1-form.test.tsx` coverage proving the Phase 1 header does not show the internal incident UUID, `Auto-save ready`, `Auto-saved`, or `Draft restored` badges.
- Ran `npm run test -- src/components/safety/incident/phase1-form.test.tsx`.
- Ran `npm run type-check`.

**Fidelity check**

- Mini-interrogation said autosave must remain active; implementation keeps `useDraftAutosave` and `saveDraftNow` intact.
- Mini-interrogation said the incident UUID should not be visible in the Phase 1 header; the badge block was removed and covered by a regression test.
- Mini-interrogation said workflow should not change; no route, API, submit, phase, or persistence code was changed.
- No deltas.

**Exemptions**

None.
