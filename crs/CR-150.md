**What & why** - Audit plan create/edit currently clears local state after a successful save, but users can still see stale form values until the route data settles. Add a page reload after successful plan save so the register reopens with clean fields and fresh server data.

**Tier + triggers** - Tier 2; changed screen state after successful Audit plan create/edit.

**Domains touched** - Domain 13 Audit UI flow.

**Decisions** - D-MAINT-CR150: use a full `window.location.reload()` after successful routine Audit plan save because the requested behavior is to clear all form state immediately after save.

**Doc cascade** - `Docs/progress.txt` updated with the shipped behavior. Canonical Audit flow docs unchanged because the underlying save contract and routes do not change.

**Tests** - `npm run test -- src/routes/audit/plans/index.test.tsx`, `npm run type-check`, and `npm run build`.

**Fidelity check** - no deltas.

**Exemptions** - none.
