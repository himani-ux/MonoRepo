**What & why** - Injury Report PDFs should place the incident-level **Describe What happened?** narrative immediately after **Reporter Details** instead of after **Injury Details**. This keeps the injury report narrative close to the reporter identity while still suppressing the legacy injury-row `what_happened_narrative` field.

**Tier + triggers** - Tier 2. Triggers: changed PDF output ordering for the existing Incident/Injury Report export surface.

**Domains touched** - Step 1 domains: incident PDF output, injury-report rendering, reporter-detail section ordering, and maintain-mode documentation.

**Decisions** - D-MAINT-CR082: when the generated title is **Injury Report**, the PDF renders **Describe What happened?** directly after **Reporter Details** and before **Injury Details**. Standard Incident Report output keeps the existing narrative placement below the intake/detail sections before **Root Cause Analysis**.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, and `safety_docsuite/VALIDATION_RULES.md` describe the Injury Report-specific narrative placement. `Docs/progress.txt` records the change. Expected docs not updated: `safety_docsuite/BACKEND_STRUCTURE.md` unchanged because no endpoint, serializer, model, or storage contract changed; `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because this is contained PDF ordering, not a workflow or structural amendment.

**Tests** - Updated `psc-backend/tests/safety/test_pdf_10_sections.py` to assert that Injury Report output places **Describe What happened?** after **Reporter Details** and before **Injury Details**, while continuing to suppress the legacy injury `what_happened_narrative`.

**Fidelity check** - no deltas. The requested Injury Report ordering is applied without changing normal Incident Report narrative placement.

**Exemptions** - none.
