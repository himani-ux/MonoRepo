**What & why** - The Safety Dashboard detail section still shows the **Reporting trend** card, which users no longer want in the simplified dashboard. This change removes that card from `/safety/dashboard/` and stops the frontend from loading the Heinrich/reporting-trend dashboard query for this screen.

**Tier + triggers** - Tier 3. Triggers: changed Safety Dashboard screen presentation and superseded current canonical dashboard wording that said Heinrich Ratio / Reporting trend remained available behind **Show details**. No endpoint, payload, permission, dependency, database, or workflow change.

**Domains touched** - Step 1 domains: Safety Dashboard presentation, optional analytics visibility, frontend route tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR139: `/safety/dashboard/` no longer displays the **Reporting trend** / Heinrich Ratio card and no longer calls the dashboard Heinrich query from this screen. Repeat issues, top causes, corrective-action age, and SOI Compliance % remain available behind **Show details**.

**Doc cascade** - Updated `safety_docsuite/PRD.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, `safety_docsuite/IMPLEMENTATION_PLAN.md`, `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`, and `Docs/progress.txt` to remove Reporting trend / Heinrich from the current dashboard presentation. Backend structure remains unchanged because the backend endpoint is not removed.

**Tests** - Updated the dashboard route test so the vessel selector assertion uses a remaining detail query instead of the removed Heinrich query. Passed `npm run test -- src/routes/safety/index.test.tsx -t dashboard`, `npm run type-check`, `npm run build`, and targeted `git diff --check`.

**Fidelity check** - no deltas.

**Exemptions** - none.
