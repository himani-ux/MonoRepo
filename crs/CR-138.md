**What & why** - The Safety Dashboard should keep the pie chart, but the current open-work donut style is not the desired look. This change keeps the same simple dashboard and same data, but changes the open-work visual to a fuller pie chart style with clearer slice separation and legend values.

**Tier + triggers** - Tier 2. Triggers: changed Safety Dashboard screen presentation. No endpoint, payload, permission, dependency, database, or workflow change.

**Domains touched** - Step 1 domains: Safety Dashboard presentation, dashboard summary chart, frontend route tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR138: `/safety/dashboard/` keeps the simplified summary-first dashboard and keeps the open-work pie chart, but renders it as a full pie chart instead of a donut chart. The chart uses the existing composite dashboard response only.

**Doc cascade** - `safety_docsuite/PRD.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, and `safety_docsuite/VALIDATION_RULES.md` were updated from donut chart wording to full pie chart wording. `safety_docsuite/IMPLEMENTATION_PLAN.md` adds Amendment 41. `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` adds D-MAINT-CR138. Backend structure remains unchanged because no backend contract changes. `Docs/progress.txt` records the change.

**Tests** - `npm run test -- src/routes/safety/index.test.tsx -t dashboard` passed. `npm run type-check` passed. `npm run build` passed with existing Vite idb/chunk-size warnings.

**Fidelity check** - no deltas. The implementation and docs all describe the same behavior: the open-work chart remains a pie chart, but now uses a full separated-slice pie style instead of a donut/ring.

**Exemptions** - none.
