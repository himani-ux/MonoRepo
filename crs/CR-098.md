**What & why** - The Certs certificate detail page currently shows only PDF metadata, and its workflow/audit card exposes internal wording such as raw action names and changed database fields. Users need to preview the active certificate PDF in the same window and read activity history in plain, non-technical language.

**Tier + triggers** - Tier 2. Triggers: new endpoint contract for inline PDF viewing, changed screen state for certificate file preview, changed user-facing wording.

**Domains touched** - Step 1 domains: Certs tracked item detail, Certs PDF blob access, Certs audit/workflow UI, Certs user guidance.

**Decisions** - Accepted default: serve tracked-item PDFs through an authenticated inline endpoint scoped by tracked item and vessel access; keep raw audit records unchanged but translate them in the UI.

**Doc cascade** - `certs_docsuite/APP_FLOW.md` updated detail layout to include inline PDF preview and user-friendly activity history. `certs_docsuite/BACKEND_STRUCTURE.md` updated tracked-item PDF view endpoint.

**Tests** - Added backend path-safety coverage for the inline PDF helper and ran focused backend/frontend checks.

**Fidelity check** - no deltas.

**Exemptions** - none.
