**What & why** - Make Incident Fleet Alert match the expected Office Review workflow: clicking **Fleet Alert** opens a vessel selection popup, the user selects vessels loaded from `VesselData`, and **Confirm** sends email/in-app alerts only to those selected vessels. SMTP sender configuration must come from environment variables, not hardcoded credentials in source.

**Tier + triggers** - Tier 2. Triggers: changed Office Review screen state from inline selector to popup confirmation, changed email configuration contract from hardcoded source values to environment-driven settings, and updated Fleet Alert validation copy/behavior.

**Domains touched** - Step 1 domains: Incident Office Review, selected-vessel Fleet Alert, `VesselData` recipient lookup, email notification delivery, frontend modal interaction, and deployment configuration.

**Decisions** - D-MAINT-CR062 accepted default: Incident Fleet Alert uses a modal popup with vessel rows from active, non-deleted `VesselData`; **Confirm** posts selected vessel IDs and sends in-app plus email only to those selected vessel email addresses. SMTP sender account and password are configured through backend environment variables. No email password is stored in tracked source.

**Doc cascade** - Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` to describe the popup confirmation and env-backed SMTP configuration, plus D-MAINT-CR062. Updated `safety_docsuite/APP_FLOW.md`, `safety_docsuite/PRD.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, `safety_docsuite/BACKEND_STRUCTURE.md`, and `safety_docsuite/TECH_STACK.md` to match the current popup and email configuration contract. Updated `psc-backend/.env.example` with non-secret email placeholders. Updated `Docs/progress.txt`. `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because no structural phase or schema plan is superseded. `Docs/LESSONS.md` unchanged because this is a requested UI/config correction, not a revert or repeated lesson miss.

**Tests** - Updated frontend Office Review regression coverage to require the Fleet Alert vessel popup and Confirm action. Added backend settings regression coverage proving email settings read environment variables and do not require hardcoded credentials.

**Fidelity check** - no deltas

**Exemptions** - none
