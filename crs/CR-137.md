**What & why** - The Safety Dashboard summary still looked more like plain bars than proper charts. This change upgrades the default summary visuals to chart-style SVG panels: an open-work donut chart and a score-breakdown horizontal chart, while keeping the dashboard simple and keeping detailed analytics behind **Show details**.

**Tier + triggers** - Tier 2. Triggers: changed Safety Dashboard screen presentation and visible layout. No endpoint, payload, permission, dependency, database, or workflow change.

**Domains touched** - Step 1 domains: Safety Dashboard presentation, dashboard summary charts, frontend route tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR137: `/safety/dashboard/` keeps the CR-135 simplified PSC-style flow and the CR-136 default summary chart area, but those summary visuals should render as proper chart panels: open-work donut chart and score-breakdown horizontal chart. The charts use the existing composite dashboard response only.

**Doc cascade** - `safety_docsuite/PRD.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, and `safety_docsuite/VALIDATION_RULES.md` describe the open-work donut chart and score-breakdown horizontal chart. `safety_docsuite/IMPLEMENTATION_PLAN.md` adds Amendment 40. `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` adds D-MAINT-CR137. Backend structure remains unchanged because no backend contract changes. `Docs/progress.txt` records the change.

**Tests** - `npm run test -- src/routes/safety/index.test.tsx -t dashboard` passed. `npm run type-check` passed. `npm run build` passed with existing Vite idb/chunk-size warnings.

**Fidelity check** - no deltas. The implementation and docs all describe the same behavior: the simplified dashboard now has actual graph/chart panels in the summary area, using existing composite data only.

**Exemptions** - none.
