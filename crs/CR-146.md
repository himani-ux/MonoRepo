**What & why** - Audit users should not have to type vessel UUIDs when creating audit records or planning audit targets. The Audit forms will keep sending the same vessel IDs to the backend, but the user-facing fields will show vessel dropdowns with readable vessel labels.

**Tier + triggers** - Tier 2. Trigger: changed screen behavior and form state for Audit vessel selection fields.

**Domains touched** - Audit registration, external audit registration, audit plan register.

**Decisions** - D-MAINT-CR146: use the existing frontend vessel option source for Audit forms and keep the existing backend request fields unchanged (`vessel_id` / `target_vessel_id`).

**Doc cascade** - Updated `audit_docsuite/BACKEND_STRUCTURE.md` with `GET /api/audit/vessels/`; updated `audit_docsuite/APP_FLOW.md` with Audit route notes and vessel dropdown behavior; updated `audit_docsuite/USER_GUIDE.md` with the user-facing vessel dropdown note. `Docs/IMPLEMENTATION_PLAN.md` unchanged because this does not change the implementation phases or structural approach.

**Tests** - Added backend regression coverage in `psc-backend/tests/audit/test_plan_api.py` proving the Audit vessel option endpoint returns readable vessel labels and plan rows render target labels from `VesselData`. Updated frontend tests in `psc-frontend/src/routes/audit/plans/index.test.tsx` and `psc-frontend/src/routes/audit/external/new.test.tsx` for vessel dropdown behavior.

**Fidelity check** - no deltas

**Exemptions** - none.
