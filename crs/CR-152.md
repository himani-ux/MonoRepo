**What & why** - Add an Audit Qualified Auditors master-maintenance screen so authorized users can create, edit, activate, and deactivate `master_audit_qualified_auditor` records through the existing gated backend API. This closes the frontend maintenance gap that left Lead Auditor dropdown data dependent on API/manual entry.

**Tier + triggers** - Tier 2. Trigger: new frontend screen and route for an existing backend contract; existing `AUDIT_P_009` permission gate is reused.

**Domains touched** - Step 1 Audit planning/master-data domain; Step 1 RBAC/navigation domain.

**Decisions** - No new product decision. Existing `AUDIT_P_009` remains the qualified-auditor management gate. The UI stores only qualification metadata and user identifiers; user identity remains resolved from the common user/crew sources.

**Doc cascade** - `audit_docsuite/GAPS_2.md` updated to state that the frontend master screen now exists while approved real/test data remains a data dependency. `audit_docsuite/resolution response.md` updated so the senior-facing response no longer says the qualified-auditor UI is missing. `Docs/progress.txt` receives the append-only Maintain entry. Expected docs unchanged: backend structure docs because the backend endpoint contract is unchanged.

**Tests** - Add focused frontend route tests for listing, creating, editing, and active/inactive toggling qualified-auditor records. Update sidebar navigation test for the new Audit child link.

**Fidelity check** - no deltas. The UI reuses the existing qualified-auditor API and `AUDIT_P_009` gate, stores only qualification metadata plus the common user identifier, and leaves real production master data as an approved-data dependency.

**Exemptions** - none.
