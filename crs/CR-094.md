**What & why**

Granted the Chief Accounting Officer profile read-only access to the VIMS/PSC module by appending all `PSC_F_001` through `PSC_F_019` form IDs to `dbo.msc_profiles.form_ids`. No `PSC_P_*` process IDs were added, so the profile can open/read VIMS screens but does not receive VIMS action authority.

**Tier + triggers**

Tier 2 - small behavior/data change. Triggers: changed local RBAC/profile permission data for one profile.

**Domains touched**

Step 1 domains: RBAC/profile permissions and local deployment data.

**Decisions**

No new decision. This applies a requested local read-only permission grant.

**Doc cascade**

No canonical docs updated because this does not change the permission model or application contract; it changes one local `msc_profiles` assignment.

**Tests**

Created backup table `dbo._codex_backup_msc_profiles_cao_readonly_20260717_130121` before update.

MCP read verification after apply confirmed Chief Accounting Officer has `PSC_F_001` through `PSC_F_019` in `form_ids`, and `process_ids` contains no `PSC_P_*` IDs.

**Fidelity check**

No deltas. The requested result was read-only VIMS access; the profile received VIMS form IDs only and no VIMS process IDs.

**Exemptions**

none
