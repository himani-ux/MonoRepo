**What & why** - Vessel-side Audit actions are not consistently visible from vessel logins because the runtime login process list can omit Audit gates that the Audit SSOT assigns to Master. This change aligns backend permission defaults and the authenticated process payload so vessel Master users can see and execute their documented Audit actions, including external vessel-audit registration.

**Tier + triggers** - Tier 2. Triggers: changed role permission behavior and frontend-visible navigation/action availability through the existing authentication process-id contract. No new endpoint, database table, migration, dependency, or analytics event is introduced.

**Domains touched** - Step 1 domains: Audit RBAC/authentication; vessel-side Audit navigation/actions; external Audit registration; Audit regression coverage; Audit user/developer documentation.

**Decisions** - D-MAINT-CR177: accepted default - align implementation with D-AUDRS-206 and the external-audit interrogation decision: vessel Master gets `AUDIT_P_013` for vessel-side external audit registration, and login/auth process IDs must include Audit default gates derived from the user's resolved role/rank so UI visibility matches backend authorization.

**Doc cascade** - Updated `audit_docsuite/USER_GUIDE.md` to state that Vessel Master and Acting Master sessions expose the documented vessel-side External Audit actions after session refresh. Updated `audit_docsuite/APP_FLOW.md` to describe auth snapshot merging for Audit process gates. Updated `audit_docsuite/BACKEND_STRUCTURE.md` to document role-derived Audit default process IDs emitted by authentication snapshots. Updated `audit_docsuite/TEST_PLAN.md` to add Master/Acting Master regression coverage. `Docs/IMPLEMENTATION_PLAN.md` unchanged because this aligns implementation with the existing D-AUDRS-206 role decision rather than amending shipped workflow scope.

**Tests** - Added focused regression coverage in `psc-backend/tests/audit/test_permissions.py` and `psc-backend/tests/audit/test_auth_permission_defaults.py`; updated the external audit registration API regression so Master succeeds through default RBAC with no explicit stored `AUDIT_P_013`. Verified with `python -m unittest tests.audit.test_permissions.AuditPermissionTests.test_default_role_designation_mapping_matches_rbac tests.audit.test_permissions.AuditPermissionTests.test_documented_role_defaults_grant_audit_gates_when_profile_seed_missing -v`, `python -m unittest tests.audit.test_auth_permission_defaults -v`, and `python -m unittest tests.audit.test_registration_api.AuditRegistrationApiTests.test_master_registers_external_vessel_audit_post_facto_at_submitted -v`. Broader `manage.py test` is blocked by the existing SQLite migration incompatibility in `inspection.0018_audit_domain_tables`, which uses SQL Server syntax.

**Fidelity check** - no deltas.

**Exemptions** - none.
