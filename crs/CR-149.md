**What & why** - Add the missing Audit module entry routes `/audit` and `/audit/dashboard`. The handover/runtime gap register identified that the application had Audit plan, registration, detail, finding, and DPA queue routes, but no Audit dashboard entry route. The new dashboard is read-only, is exposed from the existing Audit sidebar group, and uses the existing audit-plan list API so users can land in Audit without changing backend contracts.

**Tier + triggers** - Tier 2. Triggers: new frontend routes and a new screen state.

**Domains touched** - Audit frontend navigation, Audit screen inventory, Audit runtime gap tracking.

**Decisions** - D-MAINT-CR149: `/audit` redirects to `/audit/dashboard`; `/audit/dashboard` renders a read-only Audit dashboard backed by `useAuditPlans`. Route access uses the existing Audit process IDs already used by Audit plan/register/detail flows. No database migration, backend endpoint, or new permission ID is introduced.

**Doc cascade** - Updated `audit_docsuite/APP_FLOW.md` with `/audit`, `/audit/dashboard`, and the sidebar exposure. Updated `audit_docsuite/AUDIT_RUNTIME_GAPS.md` to mark the route mismatch as resolved locally. Updated `audit_docsuite/resolution response.md` with 08-19 evidence for the route fix, ORB decorator check, local master-data counts, and current open items.

**Tests** - Added frontend route tests proving `/audit/dashboard` renders plan summary data and `/audit` redirects to `/audit/dashboard`. Updated the sidebar test to prove the Audit Dashboard link appears for Audit-authorized users.

**Fidelity check** - No deltas. The implemented route resolves the specific screen inventory mismatch without adding backend behavior or changing the Audit workflow lifecycle.

**Exemptions** - none.
