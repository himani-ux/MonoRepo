**What & why** - Rename the Print certs status dropdown label from "Certificate List" to "Certificate sections" so the label clearly matches the section-based options shown in the dropdown. Share Bundle already uses "Certificate sections".

**Tier + triggers** - Tier 3. Trigger: the exact "Certificate List" label was locked in D-CERT-210 and supporting docs.

**Domains touched** - Step 1 domains: Certs print workflow label, focused frontend route tests, Certs SSOT/docsuite.

**Decisions** - D-CERT-211 supersedes D-CERT-210 only for the visible Print certs status dropdown label. The selection behavior remains section-based and unchanged.

**Doc cascade** - Update `certs_ssot/VIMS-CERTIFICATES-MODULE-SSOT.md` with D-CERT-211. Update `certs_docsuite/IMPLEMENTATION_PLAN.md`, `certs_docsuite/APP_FLOW.md`, `certs_docsuite/PRD.md`, `certs_docsuite/USER_GUIDE.md`, `certs_docsuite/FRONTEND_GUIDELINES.md`, and `Docs/LESSONS.md` for the new label. Expected unchanged: `BACKEND_STRUCTURE.md`, `FIELD_MAP.md`, and `VALIDATION_RULES.md` because payloads/validation fields remain unchanged.

**Tests** - Update focused Certs route validation tests to expect "Certificate sections" in Print certs status.

**Fidelity check** - no deltas. Current UI, tests, SSOT, and user-facing docs use "Certificate sections" for the Print certs status dropdown label.

**Exemptions** - none.
