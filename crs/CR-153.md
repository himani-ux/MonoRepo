**What & why** - Add vessel filtering to the Safety Committee Meetings and Near Miss registers so office/DPA users with global scope can narrow each register to one vessel without leaving the page.

**Tier + triggers** - Tier 2. Trigger: new frontend screen state/filter controls on existing Safety register pages.

**Domains touched** - Step 1 Safety SCM register/listing domain; Step 1 Safety Near Miss register/listing domain; Step 1 vessel scoping/filter UI domain.

**Decisions** - No new product decision. The filter is a register-view convenience only; existing backend vessel scoping and user permissions remain authoritative.

**Doc cascade** - Updated `safety_docsuite/APP_FLOW.md` to document the Vessel filter on the Near Miss and SCM list loaded states. Updated `safety_docsuite/USER_GUIDE.md` to list the DPA/global office Near Miss and SCM registers with vessel filtering. `Docs/progress.txt` receives the append-only Maintain entry. Expected docs unchanged: backend/API docs because no endpoint contract changed.

**Tests** - Added focused frontend regression tests for vessel filtering on SCM and Near Miss register pages. Passed `npm test -- src/routes/safety/index.test.tsx`. Passed `npm run type-check`.

**Fidelity check** - no deltas.

**Exemptions** - none.
