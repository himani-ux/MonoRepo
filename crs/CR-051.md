**What & why** - Add an Incident Fleet Alert action to the office-side Phase 6 Office Review screen. Office users need to send an incident alert only to selected vessels, using the same operating idea as Near Miss Fleet Alert while keeping the selected vessel list explicit and targeted.

**Tier + triggers** - Tier 2. Triggers: new Incident Office Review screen state/control, new or changed API contract for Incident Fleet Alert, targeted notification/email behavior, and validation for selected vessels.

**Domains touched** - Domain 2 screen state and role-specific Office Review UI, Domain 5 notification workflow, Domain 6 VesselData email lookup, Domain 7 API payload/response contract, Domain 9 regression tests.

**Decisions** - D-MAINT-CR051 accepted default: Incident Fleet Alert is available from office-side Phase 6 Office Review only, requires at least one selected vessel, uses `VesselData` as the vessel/email source, and sends in-app plus email alerts only to the selected vessels.

**Doc cascade** - Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` to add Incident Fleet Alert to Phase 6 Office Review, endpoint mapping, field contract, and decision ledger. Updated `safety_docsuite/APP_FLOW.md` to include the Office Review Fleet Alert control and route. Updated `safety_docsuite/PRD.md` to add FEAT-SAF-INC-042 and Office Review acceptance criteria. Updated `safety_docsuite/USER_GUIDE.md` with office-user instructions. Updated `safety_docsuite/VALIDATION_RULES.md` with selected-ship validation. Updated `safety_docsuite/BACKEND_STRUCTURE.md` with endpoint contract and no-migration note. Updated `Docs/progress.txt`. `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because no structural plan phase was superseded. `Docs/LESSONS.md` unchanged because no revert, correction, repeated lesson miss, or misclassification occurred.

**Tests** - Added `psc-backend/tests/safety/test_incident_fleet_alert.py` for selected-vessel-only in-app/email dispatch, active vessel listing, and missing-selection validation. Extended `psc-frontend/src/components/safety/incident/phase-7-workspace.test.tsx` to verify the Office Review Fleet Alert selector posts only selected vessels.

**Fidelity check** - No deltas. The implementation keeps Fleet Alert inside the office-side Office Review card, uses all active `VesselData` ships for selection, requires at least one selected ship, sends in-app notifications through `psc_notification`, sends emails from `VesselData.email`, and scopes both dispatch paths only to selected ships.

**Exemptions** - none.
