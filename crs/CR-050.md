**What & why** - Office Review should be clearer for both ship and office users. Ship-side users should see an explicit empty state when office comments or lesson learnt are not added yet, instead of a blank area. Office-side users should not see the PDF availability warning, and PDF export should not be blocked by a Phase 7 acceptance-only guard.

**Tier + triggers** - Tier 3. Triggers: changed visible Office Review screen state for ship-side users, changed PDF export validation behavior, and supersedes canonical statements that treated formal incident PDF export as available only after Phase 7 acceptance.

**Domains touched** - Domain 2 screen states and role-specific views, Domain 5 workflow/export behavior, Domain 7 validation and frontend route contract, Domain 9 regression tests.

**Decisions** - D-MAINT-CR050: Office Review comments/lesson learnt must show a user-facing pending message when no office note exists. Incident PDF export must not be blocked only because Office Review or Phase 7 acceptance is pending; the UI must not show acceptance-only PDF availability wording.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` updated the Phase 6 Office Review field contract, PDF output contract, and appended D-MAINT-CR050. `safety_docsuite/IMPLEMENTATION_PLAN.md` added Amendment 25. `safety_docsuite/APP_FLOW.md` updated Office Review states and PDF availability. `safety_docsuite/PRD.md` updated Office Review and PDF acceptance criteria. `safety_docsuite/USER_GUIDE.md` updated ship-side pending office-comment guidance and PDF preview guidance. `safety_docsuite/VALIDATION_RULES.md` removed acceptance-only PDF gate wording and added the ship-side pending state. `safety_docsuite/BACKEND_STRUCTURE.md` documented preview/download/export behavior. `Docs/LESSONS.md` added L-078 for the tier correction. `Docs/progress.txt` records this CR.

**Tests** - Updated `psc-frontend/src/components/safety/incident/phase-7-workspace.test.tsx`, `psc-backend/tests/safety/test_pdf_end_to_end.py`, and `psc-backend/tests/safety/test_msc_mepc3_pdf.py`. Passed targeted frontend Phase 7 workspace tests and backend PDF end-to-end / MSC-MEPC.3 tests.

**Fidelity check** - no deltas. Mini-interrogation domains 2/5/7/9 are covered: ship-side Office Review has an empty/pending message; office-side PDF warning text is hidden; incident PDF preview/download and MSC-MEPC.3 export no longer have an acceptance-only backend gate; regression tests cover the new visible and backend behaviors.

**Exemptions** - none.
