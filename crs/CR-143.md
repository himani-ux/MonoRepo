**What & why** - The Audit module has been developed in the handover workspace and needs to be synced into the current Complete VIMS repo. The sync must be limited to Audit-related code and docs only, because the non-Audit code in the handover workspace may be older than the current repo.

**Tier + triggers** - Tier 3. Triggers: the Audit sync adds new Audit database tables through inspection migrations, new backend endpoints under `/api/audit/`, new frontend Audit routes/screens, Audit process permission gates, Audit notification/certs/CAR integration services, and Audit operational commands.

**Domains touched** - Step 1 domains: Audit planning/registration, Audit detail/checklist/finding workflow, NC/OBS closure, external audit closeout, Audit PDF/scan validation, Audit notifications, Audit master data/seeding, Audit frontend routing/navigation, Audit permissions, Audit database migrations, tests/checks, and maintain-mode documentation.

**Decisions** - D-MAINT-CR143: Only Audit-related files from the handover workspace may be synced. Existing non-Audit files in Complete VIMS must not be overwritten from the handover unless they contain a clearly isolated Audit-specific change required for the Audit module to work.

D-MAINT-CR143A: The current repo already has `inspection` migration `0017_alter_inspectionreport_description.py`; Audit handover migrations are therefore renumbered after the current migration chain instead of overwriting existing migration history.

**Doc cascade** - Copied Audit handover docs into `audit_docsuite/`, including operational docs, release notes, user guide, validation rules, and Audit seed CSVs. Copied Audit SSOT docs into `audit_ssot/`. Added `Docs/IMPLEMENTATION_PLAN.md` Amendment 29 documenting the Audit sync boundary and migration renumbering. `Docs/progress.txt` records the sync. Non-Audit canonical docs remain unchanged because the handover's non-Audit docs are older than this repo and the request explicitly limited the sync to Audit-related code/docs.

**Tests** - Passed `python -m py_compile psc-backend/apps/inspection/workflow.py psc-backend/apps/inspection/deficiency_models.py`. Passed `python manage.py check`. Passed `python manage.py makemigrations --check --dry-run`; it reported no model changes pending, with a local SQL Server connectivity warning while checking migration history. Passed `python manage.py test tests.audit.test_model_contract tests.audit.test_permissions tests.audit.test_car_workflow_proxy -v 2` after adapting test fixtures to the current repo's vessel-code CAR numbering. Passed full Audit unittest discovery: `python -m unittest discover -s tests/audit -p "test_*.py" -v` with 157 tests. Passed frontend production build: `npm run build`.

**Fidelity check** - no deltas. Audit-owned backend, frontend, tests, migrations, seed data, and docs were synced. Shared files were merged only for Audit wiring: `/api/audit/` URL registration, Audit process IDs, Audit frontend routes, Audit sidebar entries, and Audit CAR workflow states/actions/gates. The only intentional deviation from the handover is preserving the maintained repo's current PSC CAR numbering behavior: new CARs continue to use `VESSELCODE-PSC-YEAR-NNN` and default `ALLOTTED`; Audit tests were adjusted to that current rule.

**Exemptions** - none.
