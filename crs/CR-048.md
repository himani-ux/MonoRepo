**What & why** - Seed the Phase 7 Injury Report `Code of Safe Working Practices to which the Incident relates` dropdown with the user-provided safe-working-practice options. The Loss Evaluation UI already reads this category from the injury dropdown master; this change supplies the actual master data and removes stale placeholder choices from the active dropdown.

**Tier + triggers** - Tier 3. Trigger: migration touching master data in `vims_safety_injury_dropdown_option`; visible dropdown behavior changes for the Phase 7 Loss Evaluation Injury Report state.

**Domains touched** - Domain 2 UI state, Domain 6 data layer, Domain 7 field map/master data, Domain 9 validation and tests.

**Decisions** - D-MAINT-CR048: `SAFE_WORKING_PRACTICE` options are seeded from the provided Code of Safe Working Practices list. Exact duplicate labels from the source list are stored once because the master table enforces unique `(field_key, option_label)` values and users should not see repeated dropdown rows. Existing active safe-working-practice options not in this list are deactivated by the migration.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`: records that Safe Working Practice is now seeded by CR-048 instead of future/nullable-only.
`safety_docsuite/IMPLEMENTATION_PLAN.md`: adds Amendment 23 for the seeded dropdown master data.
`safety_docsuite/BACKEND_STRUCTURE.md`: updates the injury dropdown master description to include seeded Safe Working Practice options.
`safety_docsuite/PRD.md`: changes the Injury Report Loss Evaluation field from dropdown-ready to seeded dropdown.
`safety_docsuite/VALIDATION_RULES.md`: replaces the old "options unavailable" note with seeded-option behavior.
`safety_docsuite/APP_FLOW.md`: unchanged because the phase route/state flow is unchanged.
`safety_docsuite/USER_GUIDE.md`: unchanged because user steps are unchanged; the same dropdown now has populated choices.

**Tests** - Added backend regression coverage in `tests.safety.test_phase8_loopback` verifying the seeded Safe Working Practice list appears in Loss Evaluation choices, duplicates are not repeated, and stale placeholder options are excluded. Existing Phase 7 backend/frontend checks rerun.

**Fidelity check** - no deltas. The code seeds the requested dropdown list, preserves visible order after exact-label de-duplication, deactivates stale choices, and the docs state the resulting current behavior.

**Exemptions** - none.
