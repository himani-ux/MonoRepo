**What & why**
Certs screens expose several implementation terms that are not clear to normal ship or office users. This change simplifies visible wording on the Certs landing, vessel dashboard, certificate detail metadata, filters, and certificate cards so users see certificate-focused labels instead of internal terms like tracked item, catalog code, validity type, PDF, and hierarchy.

**Tier + triggers**
Tier 2 - small behavior/UI wording change. Triggers: changed screen copy and changed user-facing screen state.

**Domains touched**
Step 1 domains touched: Certs frontend user experience and Certs documentation.

**Decisions**
No new SSOT decision planned. Accepted default: preserve backend/API field names and admin-only technical controls, but simplify labels on normal user-facing Certs screens.

**Doc cascade**
Updated `certs_docsuite/APP_FLOW.md` so the vessel dashboard and certificate detail sections describe the simplified user-facing wording, certificate file wording, and collapsed technical details.
Expected docs not updated: backend/database docs unchanged because this is a frontend wording-only change with no API, table, or workflow contract change.

**Tests**
`npm run type-check` passed in `psc-frontend`.
`npm run build` passed in `psc-frontend`; existing Vite large-chunk/idb warnings remain.

**Fidelity check**
No deltas. The docs state the same boundary as the implementation: normal vessel/certificate screens use certificate-focused wording, while backend/API/admin concepts remain unchanged.

**Exemptions**
None.
