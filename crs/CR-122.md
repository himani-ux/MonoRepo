**What & why** - Rename the normal Certs print screen and vessel-dashboard action from "Print Vessel Status" to "Print certs status" so the label matches the user's preferred wording while keeping the simplified current-vessel print workflow unchanged.

**Tier + triggers** - Tier 3. Trigger: the visible label contradicts the locked D-CERT-208 label and Amendment 9 wording.

**Domains touched** - Step 1 domains: Certs print workflow labels, vessel dashboard print action label, Certs SSOT/docsuite, focused frontend route tests.

**Decisions** - D-CERT-209 supersedes D-CERT-208 only for the visible label. The label is "Print certs status"; all D-CERT-208 behavior remains unchanged.

**Doc cascade** - Update `certs_ssot/VIMS-CERTIFICATES-MODULE-SSOT.md` with D-CERT-209. Update `certs_docsuite/IMPLEMENTATION_PLAN.md` with Amendment 10. Update `certs_docsuite/APP_FLOW.md`, `certs_docsuite/BACKEND_STRUCTURE.md`, `certs_docsuite/FIELD_MAP.md`, `certs_docsuite/PRD.md`, `certs_docsuite/USER_GUIDE.md`, and `certs_docsuite/FRONTEND_GUIDELINES.md` for the new label. Update `Docs/LESSONS.md` because the label preference corrected the previous wording. Expected unchanged: `TECH_STACK.md` because no dependency/runtime changes; `VALIDATION_RULES.md` because no validation behavior changed.

**Tests** - Update focused Certs route validation tests to expect "Print certs status" and keep the removed field assertions.

**Fidelity check** - no deltas. Current docs and tests use "Print certs status" for the visible label while preserving the D-CERT-208 simplified print behavior.

**Exemptions** - none.
