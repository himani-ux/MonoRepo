**What & why** - Incident PDFs should not print Last port because the current Incident Phase 1 frontend no longer shows or sends that field. Keeping it in the PDF can expose stale legacy data that users cannot maintain in the current incident flow.

**Tier + triggers** - Tier 2; the generated Incident PDF contract changes.

**Domains touched** - Safety incident PDF generation, Phase 1 reporting-context PDF output, injury detail PDF fallback output, and safety documentation.

**Decisions** - Added `D-MAINT-CR070` as an append-only SSOT decision. It supersedes `D-MAINT-CR032` only where Last Port remained a current PDF-fallback field.

**Doc cascade** - `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` adds `D-MAINT-CR070` and updates Phase 1/PDF Last Port wording. `safety_docsuite/APP_FLOW.md`, `safety_docsuite/PRD.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, and `safety_docsuite/BACKEND_STRUCTURE.md` now state that Last Port is legacy storage only and is not printed in the current Incident PDF. `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because no Tier 3 amendment is required. Other canonical docs unchanged because they do not describe current Incident PDF Last Port output.

**Tests** - Updated `tests.safety.test_pdf_10_sections` to save legacy `last_port` values on incident/injury fixtures and assert the generated Incident PDF does not print a Last port row or stale value.

**Fidelity check** - no deltas. Mini-interrogation findings match implementation: `last_port` columns remain readable compatibility storage, current Phase 1 frontend continues omitting the field, and the PDF no longer surfaces legacy Last port data.

**Exemptions** - None.
