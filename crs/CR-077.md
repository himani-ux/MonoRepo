**What & why** - Office users need the Safety dashboard to be easier to scan. The dashboard will show the essential score, repeat issues, top causes, corrective-action age, and export controls first, while moving advanced cards such as Heinrich Ratio and SOI Compliance % behind an optional **Show more dashboard cards** control. Dashboard wording is simplified so users see plain labels before technical analytics terms.

**Tier + triggers** - Tier 3. Triggers: changed Safety dashboard screen state, changed dashboard information hierarchy for office users, and superseded prior canonical wording that made Heinrich/SOI dashboard panels always visible.

**Domains touched** - Domain 6 Safety dashboard, Domain 7 frontend route behavior, Domain 8 dashboard/analytics presentation, Domain 9 regression tests and docs.

**Decisions** - D-MAINT-CR077 supersedes D-GAP-M27 and D-GAP-DESIGN-01 only where they required Heinrich Ratio and SOI Compliance % to be immediately visible on the main office dashboard. The metrics remain available, but they are hidden by default in an optional advanced dashboard section. The literal **SOI Compliance %** label remains unchanged wherever that metric is shown.

**Doc cascade** - Updated `safety_docsuite/IMPLEMENTATION_PLAN.md` with Amendment 32 for the dashboard simplification. Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` with D-MAINT-CR077. Updated `safety_docsuite/APP_FLOW.md`, `safety_docsuite/PRD.md`, `safety_docsuite/USER_GUIDE.md`, and `safety_docsuite/VALIDATION_RULES.md` to describe the optional advanced cards and simpler dashboard wording. Updated `Docs/progress.txt`. Expected docs not updated: `safety_docsuite/BACKEND_STRUCTURE.md` unchanged because the existing dashboard endpoints and payloads are unchanged.

**Tests** - Updated `psc-frontend/tests/frontend/safety/dashboard-route.test.tsx` to prove Heinrich Ratio and SOI Compliance % are hidden by default and appear after the user clicks **Show more dashboard cards**.

**Fidelity check** - no deltas. The user asked to simplify the office dashboard, hide cards like Heinrich Ratio and SOI Compliance % until the user chooses to see them, and make the language easier; the implementation keeps those metrics available while moving them out of the default view and simplifying user-facing labels/copy.

**Exemptions** - none.
