**What & why** - Near Miss Fleet Alert currently records the fleet alert and emits in-app notifications, but users need the same delivery behavior as Incident Fleet Alert: selected vessels receive in-app notification plus email, with `HSSEQ@kaizenship.net` copied. This keeps high-priority near-miss fleet learning visible in VIMS and delivered by email from the same configured backend sender.

**Tier + triggers** - Tier 3. Triggers: adds SMTP email delivery to the existing Near Miss Fleet Alert workflow, which is an external integration on that workflow and changes the endpoint response payload to include email-delivery counts.

**Domains touched** - Step 1 domains: Near Miss high-priority Fleet Alert, selected-vessel recipient resolution, notification/email delivery, backend API response, and maintain-mode documentation.

**Decisions** - D-MAINT-CR083: issuing a Near Miss Fleet Alert sends in-app `NEAR_MISS_FLEET_ALERT` notifications and one batched email to selected vessel `VesselData.email` recipients using BCC, with `HSSEQ@kaizenship.net` in CC. Missing selected-vessel email or missing SMTP credentials blocks issue before notifications/history are written.

**Doc cascade** - `safety_docsuite/IMPLEMENTATION_PLAN.md` adds Amendment 35. `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`, `safety_docsuite/PRD.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, and `safety_docsuite/BACKEND_STRUCTURE.md` describe near-miss fleet-alert email delivery and response counts. `Docs/progress.txt` records the change.

**Tests** - Update `psc-backend/tests/safety/test_fleet_alert_issue.py` so Near Miss Fleet Alert proves in-app notification rows and a single batched SMTP email with selected vessel recipients in BCC and `HSSEQ@kaizenship.net` in CC.

**Fidelity check** - no deltas. The requested behavior is implemented as in-app notification plus email for the existing Near Miss Fleet Alert issue action, matching Incident Fleet Alert's selected-vessel email pattern.

**Exemptions** - none.
