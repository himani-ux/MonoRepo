**What & why** - Remove internal print metadata from the generated Certs Excel workbook so the downloaded spreadsheet matches the cleaned user-facing print output. `print_id`, scope, and system-state hash remain stored in artifact history, audit records, filenames, and API responses; they are only removed from visible Excel sheet content.

**Tier + triggers** - Tier 3. Trigger: visible print artifact identity fields are locked in the original print/export decisions and already superseded for normal PDFs by D-CERT-202; Excel now needs the same visible-output rule.

**Domains touched** - Step 1 domains: Certs print/export artifact rendering, print history/audit traceability, backend renderer tests, Certs SSOT/docsuite.

**Decisions** - D-CERT-212 supersedes D-CERT-128, D-CERT-141, D-CERT-147, and D-CERT-202 only where they could be read as requiring `print_id`, scope, or system-state hash to appear inside the visible Excel workbook. Stored metadata and artifact traceability remain unchanged.

**Doc cascade** - Update `certs_ssot/VIMS-CERTIFICATES-MODULE-SSOT.md` with D-CERT-212. Update `certs_docsuite/IMPLEMENTATION_PLAN.md`, `certs_docsuite/APP_FLOW.md`, `certs_docsuite/PRD.md`, `certs_docsuite/FIELD_MAP.md`, `certs_docsuite/BACKEND_STRUCTURE.md`, `certs_docsuite/DESIGN_SYSTEM.md`, `certs_docsuite/SECURITY.md`, `certs_docsuite/USER_GUIDE.md`, and `Docs/LESSONS.md` to state that normal visible Excel workbooks omit these internal identifiers while stored audit/history metadata remains. Expected unchanged: frontend guidelines and validation rules because the UI form and API payload contract do not change.

**Tests** - Add a renderer regression test that opens the generated Excel workbook and asserts `Print ID`, `Scope`, `System state hash`, and their values are absent while certificate row data remains.

**Fidelity check** - no deltas. The current code, tests, and docs preserve artifact metadata outside the visible Excel workbook and remove only the workbook rows.

**Exemptions** - none.
