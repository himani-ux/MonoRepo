**What & why**

Synced the local SQL Server `dbo.master_role` and `dbo.msc_profiles` tables from the exported scripts `master_role.sql` and `msc_profiles.sql` without overwriting existing local profile data. The sync inserted only missing profile rows and appended missing `form_ids` / `process_ids` into existing profile JSON arrays, preserving all existing local IDs and permission entries.

**Tier + triggers**

Tier 2 - small behavior/data change. Triggers: changed local role/profile permission data used by application RBAC.

**Domains touched**

Step 1 domains: RBAC/profile permissions and local deployment data.

**Decisions**

No new decision. This was a local database synchronization from provided export scripts.

**Doc cascade**

No canonical docs updated because this change synchronizes local database seed/permission data and does not change the documented RBAC model or application contract.

**Tests**

Dry-run comparison before apply: `master_role` had 0 missing rows; `msc_profiles` had 2 missing rows, 109 missing `form_ids`, and 622 missing `process_ids`.

Apply created backup tables `dbo._codex_backup_master_role_20260717_124108` and `dbo._codex_backup_msc_profiles_20260717_124108`, inserted 2 `msc_profiles` rows, and appended the missing permission IDs.

Post-apply dry-run returned 0 remaining inserts, 0 remaining `form_ids` additions, and 0 remaining `process_ids` additions.

MCP read verification after apply: `dbo.master_role = 33`, `dbo.msc_profiles = 52`, backup `master_role = 33`, backup `msc_profiles = 50`.

**Fidelity check**

No deltas. Existing local `msc_profiles` rows were not replaced; only missing permission IDs were appended, and only missing profile rows from the export were inserted.

**Exemptions**

none
