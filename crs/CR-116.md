**What & why** - The class reconciliation review page needs clearer user-facing wording, fewer zero-value review cards, working access to the uploaded class status PDF, and readable review-action comparison values. Vessel-side users also need to open the class status PDF from office messages so they can verify what office reviewed.

**Tier + triggers** - Tier 2 small feature/behavior change. Triggers: changed existing review screen state, changed vessel-side review message behavior, changed use of existing class snapshot PDF view contract, and new regression coverage.

**Domains touched** - Step 1 domains: Certs class reconciliation review UI, office review action display, vessel-side office message display, class snapshot PDF access, frontend/backend regression tests, and Certs documentation.

**Decisions** - No new SSOT decision expected. Accepted default: hide the zero-value Extended/Postponed and Needs manual check cards from the normal review tab row while preserving the buckets in backend data for compatibility; open class report actions use the existing uploaded snapshot PDF endpoint instead of creating a new storage path.

**Doc cascade** - Updated `certs_docsuite/APP_FLOW.md` for renamed review cards, hidden empty extension/manual-check groups, enabled class report PDF access, readable review-action values, and vessel-side class status PDF access. Updated `certs_docsuite/BACKEND_STRUCTURE.md` to document `GET /api/certs/class-snapshots/<id>/pdf/view/`. Updated `certs_docsuite/USER_GUIDE.md` for office and vessel-side class report PDF workflows and cleaner review wording. Updated `certs_docsuite/FIELD_MAP.md` for `snapshotId`/`pdfBlobId` PDF links and formatted diff display. Expected unchanged: `certs_docsuite/IMPLEMENTATION_PLAN.md` unchanged because this refines existing reconciliation/PDF-access behavior without a structural change.

**Tests** - Added backend route/view regression coverage for the class snapshot PDF inline endpoint. Updated frontend guard tests for renamed review cards, hidden empty extension/manual-check cards, enabled class report PDF links, formatted diff values, and vessel-side class status PDF access.

**Fidelity check** - no deltas.

**Exemptions** - none.
