**What & why** - Phase 6 Office Review PDF export should no longer ask users to choose every printable section. All normal incident PDF content is compulsory, and only the Phase 7 Loss Evaluation content is optional through a single **Print Loss Evaluation** checkbox. The PDF action label is changed to **Download PDF** to match the user-facing action.

**Tier + triggers** - Tier 2. Triggers: changed visible screen state on Phase 6 Office Review and changed frontend PDF export payload behavior.

**Domains touched** - Domain 2 screen state and user actions, Domain 7 frontend/backend PDF export contract, Domain 9 regression tests and docs.

**Decisions** - D-MAINT-CR058 accepted user direction: Phase 6 Office Review always exports the mandatory incident PDF sections. The only user-facing PDF content choice is whether to include Loss Evaluation, mapped to the existing backend `estimated_cost` section key.

**Doc cascade** - Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` for the current Phase 6 PDF option. Updated `safety_docsuite/APP_FLOW.md`, `safety_docsuite/PRD.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, and `safety_docsuite/BACKEND_STRUCTURE.md` for the mandatory section export plus optional Loss Evaluation checkbox. Updated `Docs/progress.txt`.

**Tests** - Added Phase 6 Office Review frontend regression tests that the old multi-section selector is absent, the action is named Download PDF, unchecked export omits `estimated_cost`, and checked export includes `estimated_cost`.

**Fidelity check** - no deltas. The implementation keeps all non-Loss-Evaluation sections compulsory in the request payload and exposes only the Loss Evaluation checkbox.

**Exemptions** - none.
