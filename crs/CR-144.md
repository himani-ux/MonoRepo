**What & why** - The Audit handover workspace received gap-resolution updates after the first Audit sync. This change safely imports only the relevant Audit gap evidence and the small runtime fixes that are needed in the current repo: the Audit create-page permission gate and the ORB decorator security correction called out by the resolution notes.

**Tier + triggers** - Tier 3. Triggers: shared backend authentication behavior changes in ORB function views, frontend route permission behavior changes for Audit creation, and Audit review/gap documentation updates.

**Domains touched** - Step 1 domains: Audit frontend route permissions, Audit runtime gap documentation, Audit mock approval evidence, shared ORB API auth behavior, tests/checks, and maintain-mode documentation.

**Decisions** - D-MAINT-CR144: Sync only the implemented gap-resolution deltas from the handover workspace. Do not copy the source workspace's broad dependency updates, older non-Audit files, or unrelated PSC/Safety changes.

D-MAINT-CR144A: Preserve `PyPDF2==3.0.1` in this repo because the current codebase still imports `PyPDF2` in Certs, Circular, and Safety paths. The source workspace's `pypdf` dependency drift is not synced.

D-MAINT-CR144B: Treat the live `msc_profiles` permission update described in the handover gap note as an operational record only. No database update is run in this repo as part of this sync.

**Doc cascade** - Updated `audit_docsuite/AUDIT_RUNTIME_GAPS.md` from the handover's revised `AUDIT_GAPS.md`. Added `audit_docsuite/MOCK_APPROVAL.md`, `audit_docsuite/MOCK_COVERAGE_GAPS.md`, and approved reference mock HTML files under `audit_docsuite/mockups/`. Added `Docs/IMPLEMENTATION_PLAN.md` Amendment 30 and recorded the change in `Docs/progress.txt`. Existing non-Audit docs are unchanged because the sync is intentionally limited to Audit gap-resolution material.

**Tests** - Passed `python -m py_compile apps\inspection\workflow.py modules\orb\orb\views.py`. Passed `python manage.py check`. Passed focused Audit backend tests: `python manage.py test tests.audit.test_permissions tests.audit.test_car_workflow_proxy -v 2` with 17 tests. Passed frontend production build: `npm run build`. Manual scan confirmed the four named ORB function views no longer carry `@permission_classes([AllowAny])`.

**Fidelity check** - no deltas. The current repo receives the handover's safe functional deltas: Audit users with `AUDIT_P_001` or `AUDIT_P_003` can access the existing create-inspection route used for Audit registration, and the four ORB endpoints no longer activate `AllowAny`. The source workspace's dependency bump to `pypdf` is intentionally not synced because it contradicts this repo's current imports and the earlier resolution guidance for this baseline.

**Exemptions** - none.
