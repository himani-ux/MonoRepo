**What & why** - The Safety Dashboard **Score breakdown** card is visually acceptable but still hard for users to understand because it shows raw component numbers on a chart axis. This change keeps the same backend scores and turns the card into a plain ranked health list that highlights which part is pulling the score down most.

**Tier + triggers** - Tier 2. Triggers: changed Safety Dashboard card presentation and frontend interpretation logic. No endpoint, payload, database, permission, dependency, or workflow change.

**Domains touched** - Step 1 domains: Safety Dashboard presentation, score component explanation, frontend route tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR140: `/safety/dashboard/` shows Score breakdown as a plain ranked score-health list using the existing composite `component_scores` payload. Lower scoring components are displayed first and labelled with simple statuses. The backend score calculation remains unchanged.

**Doc cascade** - Updated `safety_docsuite/PRD.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/VALIDATION_RULES.md`, `safety_docsuite/IMPLEMENTATION_PLAN.md`, `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`, and `Docs/progress.txt` to describe Score breakdown as a ranked score-health list instead of a raw chart. Backend docs remain unchanged because the backend calculation and API contract are unchanged.

**Tests** - Updated the dashboard route test to assert the new Score breakdown wording. Passed `npm run test -- src/routes/safety/index.test.tsx -t dashboard`, `npm run type-check`, `npm run build`, and targeted `git diff --check`.

**Fidelity check** - no deltas.

**Exemptions** - none.
