**What & why** - Phase 7 Loss Evaluation now first asks the user whether they are recording the evaluation as an Incident Report or an Injury Report. The chosen type controls which field group is shown, saved, reloaded, and printed in the Loss Evaluation cost/detail PDF block, so users are not forced into the automatic injury-detection layout.

**Tier + triggers** - Tier 2. Triggers: changed Phase 7 screen state, changed Loss Evaluation API payload/response contract, likely new nullable persistence for the selected report type, and regression tests for the changed behavior.

**Domains touched** - Domain 2 screen state and user choice flow, Domain 4 workflow behavior, Domain 7 API payload/response contract and persistence, Domain 9 regression tests.

**Decisions** - D-MAINT-CR053 accepted default: Phase 7 asks for the Loss Evaluation report type first with Incident Report and Injury Report choices; the chosen type is persisted with the Loss Evaluation record and drives the visible fields on reload, while existing records without a saved choice keep the previous automatic default.

**Doc cascade** - Updated `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` to add D-MAINT-CR053, document the Phase 7 report-type selector, and note `report_type` persistence/PDF fallback behavior. Updated `safety_docsuite/APP_FLOW.md` to show the Report Type selector as the first Phase 7 state and correct the save/close gates. Updated `safety_docsuite/PRD.md` to include D-MAINT-CR053 acceptance criteria. Updated `safety_docsuite/USER_GUIDE.md` to tell users to choose Incident Report or Injury Report before completing Loss Evaluation. Updated `safety_docsuite/VALIDATION_RULES.md` with V-INC-096 for missing/invalid report type. Updated `safety_docsuite/BACKEND_STRUCTURE.md` with migration `0056_incident_loss_evaluation_report_type.py`, table/API changes, and PDF behavior. Updated `Docs/progress.txt`. `safety_docsuite/IMPLEMENTATION_PLAN.md` unchanged because the existing Loss Evaluation phase remains the same phase and this only adds a selector to the current implementation. `Docs/LESSONS.md` unchanged because no revert, user-correction recurrence, or misclassification occurred.

**Tests** - Added/updated backend tests in `psc-backend/tests/safety/test_phase8_loopback.py` for saving explicit `INCIDENT`, choosing `INJURY` without an injury row, and preserving the legacy injury fallback for rows without `report_type`. Added PDF regression coverage in `psc-backend/tests/safety/test_pdf_10_sections.py` proving saved `report_type` overrides injury presence for Loss Evaluation PDF blocks. Updated frontend tests in `psc-frontend/src/components/safety/incident/phase-8-workspace.test.tsx` to verify fields stay hidden until a type is chosen, Incident Report selection shows incident fields, Injury Report selection shows injury fields, and save payload includes `report_type`. Ran `python -m unittest tests.safety.test_phase8_loopback tests.safety.test_pdf_10_sections.IncidentPdfSectionTests.test_pdf_estimated_cost_prints_loss_evaluation tests.safety.test_pdf_10_sections.IncidentPdfSectionTests.test_pdf_loss_evaluation_uses_saved_report_type_over_injury_presence -v` and `npm test -- --run src/components/safety/incident/phase-8-workspace.test.tsx`; both passed.

**Fidelity check** - no deltas. The accepted default required first choosing Incident Report or Injury Report, persisting the choice, driving visible fields on reload/PDF, and keeping an old-record fallback. Code and docs now include those details.

**Exemptions** - none.
