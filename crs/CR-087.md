**What & why**

Sync the Certs module from `C:\Users\himan\Desktop\VIMS-CERTS-HANDOVER` into the current repository without importing unrelated stale VIMS-with-Safety code from that source tree. The goal is to bring only certificate-management functionality into the maintained project while preserving the current Safety/PSC state.

**Tier + triggers**

Tier 3 - structural. Triggers: module sync from another codebase, likely new/changed frontend routes, backend endpoints/contracts, possible migrations or schema-bearing code, and cross-module integration into shared app navigation/build/runtime.

**Domains touched**

Step 1 domains touched: application module boundaries, navigation/routing, backend API contracts, database/migrations, role/access behavior, deployment/build behavior, and user-facing documentation.

**Decisions**

Accepted default: Certs is synced as a separate module using the handover module files only. Shared app files in the stale source tree were not copied wholesale; current PSC, Safety, Circular, ORB, login, and shared navigation behavior remain authoritative.

Accepted default: Certs documentation is kept under `certs_docsuite/` and `certs_ssot/` so it is visible in this repo without mixing Certs behavior into the Safety SSOT.

**Doc cascade**

Updated `safety_docsuite/IMPLEMENTATION_PLAN.md` with Amendment 37 describing Certs as a separate synced module and naming the current backend/frontend integration points.

Added Certs canonical docs from the handover source under `certs_docsuite/`: `APP_FLOW.md`, `BACKEND_STRUCTURE.md`, `COVERAGE.md`, `DESIGN_SYSTEM.md`, `FIELD_MAP.md`, `FRONTEND_GUIDELINES.md`, `IMPLEMENTATION_PLAN.md`, `OBSERVABILITY.md`, `PRD.md`, `SECURITY.md`, `TECH_STACK.md`, `USER_GUIDE.md`, and `VALIDATION_RULES.md`.

Added Certs SSOT under `certs_ssot/VIMS-CERTIFICATES-MODULE-SSOT.md`.

Unchanged because this sync does not alter Safety behavior: `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md`, `safety_docsuite/APP_FLOW.md`, `safety_docsuite/USER_GUIDE.md`, `safety_docsuite/BACKEND_STRUCTURE.md`, and `safety_docsuite/VALIDATION_RULES.md`.

**Tests**

Added `psc-backend/apps/certs/tests.py` to assert the Certs app is registered and `/api/certs/` plus `/api/auditor/` routes resolve.

Passed:
- `python manage.py test apps.certs.tests -v 2`
- `python manage.py check`
- `python manage.py makemigrations --check --dry-run` (`No changes detected`; emitted a SQL Server connectivity warning because local SQL Server was not reachable from this shell)
- `npm run build`

Dependency note: `pdfplumber` and `pytesseract` import successfully in the local Python environment. `slack_sdk` is listed in `psc-backend/requirements.txt` for deployment, but is not installed in the current local Python environment; Certs Slack relay has an import fallback until requirements are installed.

**Fidelity check**

No deltas. The copied Certs module paths match the copied Certs docs/SSOT at module level: backend `apps.certs`, frontend `/certs/*`, public auditor `/auditor/*`, Certs form/process IDs, and Certs deployment dependencies are present in code and docs. Shared stale-source PSC/Safety files were intentionally not copied; the current repository's shared files were patched only at the integration points needed for Certs.

**Exemptions**

None.
