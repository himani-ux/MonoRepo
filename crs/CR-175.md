**What & why** - SCM meeting hosting currently lets non-numeric latitude or longitude text pass the frontend submit guard when a port/place is entered, so the backend DecimalField validation can surface the generic message "A valid number is required" after the user clicks Submit to office. The form fields and coordinate storage format stay unchanged; the validation response now tells users to enter decimal-degree coordinates such as `1.290270` and `103.851959`, using a minus sign for south or west instead of N/S/E/W letters.

**Tier + triggers** - Tier 2. Trigger: changed client validation rule for SCM create/edit coordinate fields.

**Domains touched** - Step 1 Safety SCM meeting workflow; Safety frontend validation/error handling; Safety validation documentation.

**Decisions** - D-MAINT-CR175: SCM Latitude and Longitude remain optional when Location is entered, and remain required as a pair when Location is blank. When either coordinate field is nonblank, the frontend validates it as decimal-degree text before calling the SCM create/update API, and both frontend and backend return field-specific format guidance instead of the generic numeric error.

**Doc cascade** - `safety_docsuite/VALIDATION_RULES.md` records the SCM coordinate decimal-degree validation messages. `safety_docsuite/USER_GUIDE.md` clarifies that Location can be used instead of coordinates and gives decimal-degree examples for typed coordinates. Expected docs unchanged: `safety_docsuite/BACKEND_STRUCTURE.md` because the backend API fields and database columns do not change; `safety_docsuite/APP_FLOW.md` because the SCM screen/workflow stays the same.

**Tests** - Added `psc-frontend/src/schemas/safety/scm.test.ts` and backend assertion `test_regular_create_invalid_coordinate_explains_decimal_degrees_format`. Verified pre-fix failure for non-numeric latitude. Verified post-fix with `npm test -- src/schemas/safety/scm.test.ts` and backend coordinate compatibility/error-message tests with `python -m unittest tests.safety.test_scm_regular_crud.SCMRegularCrudTests.test_regular_create_accepts_at_sea_coordinates_without_location tests.safety.test_scm_regular_crud.SCMRegularCrudTests.test_regular_create_invalid_coordinate_explains_decimal_degrees_format tests.safety.test_scm_regular_crud.SCMRegularCrudTests.test_co_can_create_regular_scm_and_read_it_back -v`.

**Fidelity check** - no deltas

**Exemptions** - none.
