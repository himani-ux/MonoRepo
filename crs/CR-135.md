**What & why** - The Safety Dashboard is still visually heavy for daily users. This change simplifies `/safety/dashboard/` to a PSC-style summary-first page with compact filters, clear KPI cards, a quieter details toggle, and the same export action. The data, API calls, permissions, and navigation behavior remain unchanged.

**Tier + triggers** - Tier 2. Triggers: changed Safety Dashboard screen presentation and visible wording/layout, with frontend tests/docs updated for the changed UI contract.

**Domains touched** - Step 1 domains: Safety Dashboard presentation, dashboard filters, optional analytics visibility, dashboard export placement, frontend route tests, and maintain-mode documentation.

**Decisions** - D-MAINT-CR135: `/safety/dashboard/` should match the PSC dashboard pattern more closely: a simple title, compact scope/period/vessel controls, immediate KPI cards for the current work, and a single optional **Show details** area for repeat issues, top causes, overdue-action age, reporting trend, and SOI check status. Existing calculations, endpoints, export permissions, and detailed analytics remain unchanged.

**Doc cascade** - `safety_docsuite/PRD.md` records the summary-first dashboard requirement. `safety_docsuite/APP_FLOW.md` records the simpler dashboard route flow. `safety_docsuite/USER_GUIDE.md` explains the compact cards and **Show details** control. `safety_docsuite/VALIDATION_RULES.md` records that the detailed analytics may sit behind **Show details**. `safety_docsuite/IMPLEMENTATION_PLAN.md` adds Amendment 38 for the dashboard presentation change. `safety_ssot/VIMS-SAFETY-MODULE-SSOT.md` adds D-MAINT-CR135 as the superseding dashboard presentation decision. `safety_docsuite/BACKEND_STRUCTURE.md` unchanged because no backend route, table, payload, or permission contract changes. `Docs/progress.txt` records the change.

**Tests** - `npm run test -- src/routes/safety/index.test.tsx -t dashboard` passed. `npm run type-check` passed. `npm run build` passed with existing Vite chunk/import warnings. Full `src/routes/safety/index.test.tsx` still has unrelated SCM expectations failing around SCM register text and closed-since-previous-SCM copy; dashboard tests passed.

**Fidelity check** - no deltas. The code, PRD, app flow, user guide, validation rules, SSOT decision, and implementation amendment all describe the same behavior: compact summary cards first, filters in the header, optional details, and unchanged backend/export behavior.

**Exemptions** - none.
