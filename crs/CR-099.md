**What & why** - The Certs certificate detail page should not automatically load and display the active PDF. Users only need a clear view option, with the PDF name acting as the link that opens the document when they choose to view it.

**Tier + triggers** - Tier 2. Triggers: changed Certs detail screen state and user-facing PDF viewing behavior.

**Domains touched** - Step 1 domains: Certs tracked item detail, Certs PDF blob access, Certs user guidance.

**Decisions** - Accepted default: keep the authenticated PDF endpoint from CR-098, but fetch and open the PDF only when the user clicks the file name or view action; do not embed the preview by default.

**Doc cascade** - `certs_docsuite/APP_FLOW.md` updated detail layout to describe on-demand PDF viewing instead of automatic inline preview. `certs_docsuite/BACKEND_STRUCTURE.md` renamed the PDF endpoint purpose from preview to authenticated PDF view stream.

**Tests** - Ran focused backend PDF path tests, Python compile, frontend type-check, and frontend production build.

**Fidelity check** - no deltas.

**Exemptions** - none.
