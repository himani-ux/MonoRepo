**What & why** - Certs was mostly aligned with the maintained docs, but the audit found three high-risk drifts: Certs notifications could still fail when the shared `master_notification` table did not expose the Certs columns, the background-job docs still described Celery/beat wiring that this deployment does not prove, and the original SSOT print clauses needed an explicit supersession trail after the approved clean-PDF changes. This change fixes the notification schema compatibility and updates the canonical Certs docs so the shipped system, deployment model, and audit trail say the same thing.

**Tier + triggers** - Tier 3. Triggers: migration touching a shared notification table, cross-module notification contract alignment, and implementation-plan amendment superseding older canonical statements.

**Domains touched** - Certs notifications, parser/reconciliation anomaly reporting, background jobs/deployment operations, print/export SSOT supersession, backend tests, canonical Certs documentation.

**Decisions** - D-CERT-203: Certs may add nullable compatibility columns to the shared `master_notification` table when those columns are absent, preserving existing rows while allowing `vims_certs_notification_meta` to keep its master-notification link. D-CERT-204: until platform Celery/beat wiring is explicitly deployed for Certs, the current deployment truth is management-command/job entry points scheduled by the host/server scheduler; docs must name the actual entry points and not claim unproven beat registration.

**Doc cascade** - Updated `certs_docsuite/IMPLEMENTATION_PLAN.md` with Amendment 6 for D-CERT-203 and D-CERT-204; updated `certs_docsuite/TECH_STACK.md` to describe guarded `master_notification` compatibility and management-command scheduler truth; updated `certs_docsuite/BACKEND_STRUCTURE.md` to name the actual Certs job files and command entry points; updated `certs_docsuite/APP_FLOW.md` to describe the compatibility-backed Certs notification inbox behavior; updated `Docs/progress.txt`. `certs_ssot/VIMS-CERTIFICATES-MODULE-SSOT.md` unchanged because maintain-mode supersessions are append-only in the implementation plan rather than edits to the original locked SSOT.

**Tests** - Added a backend regression test asserting the guarded migration covers every Certs-required `master_notification` column with `COL_LENGTH` checks. Existing Certs backend/frontend tests remain the verification gate.

**Fidelity check** - no deltas. The implementation-plan amendment states the current shipped truth: clean print PDFs remain governed by D-CERT-202, Certs notification metadata keeps linking to `master_notification`, and actual Certs scheduled work is exposed through the current job/management-command entry points until a separate Celery deployment change is made.

**Exemptions** - none
