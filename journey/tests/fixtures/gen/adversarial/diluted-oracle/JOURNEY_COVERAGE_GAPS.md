# JOURNEY_COVERAGE_GAPS — adversarial diluted-oracle fixture (empty gap set)
#
# No gaps: every required id is journeyed. The defect under test is oracle
# *dilution* inside JOURNEY-101, not a coverage hole — so the coverage gate
# passes and the dilution must be caught elsewhere (golden diff / refuter).
