2026-07-10 | Incident PDF witness output | A repeated user correction confirmed that current PDF output must not print legacy/stale fields simply because database columns contain values. For Witness Statement, only current UI-owned output belongs in the PDF: witness block heading, downloadable attachment link when present, and Remark. The old free-text `What the witness said` value must stay suppressed even for legacy populated rows.
2026-07-13 | Phase 1 payload builders | User correction exposed that new Phase 1 fields were added to the shared form but not to every route-level payload builder. When adding visible fields, update and test both create and edit route mappers, not just the form schema/backend serializer.
2026-07-13 | Incident rework target selection | User correction confirmed Incident Office Review rework must stay as one free-text instruction box, not a target-phase selector. When rework instructions need to be more visible, expose and render the saved phase-log comment instead of adding phase-picking UI.
2026-07-13 | Incident action simplification | User correction confirmed that removing status/open-close/verification controls from Phase 3/4 actions must not remove Due Date. When simplifying a form, classify each field separately as tracking/control versus recording data, and verify PDF/source-data requirements before hiding it.
2026-07-13 | Safety sidebar navigation | User correction showed that removing a Safety navigation item from `SafetySidebarGroup` is not enough when the visible app shell uses `components/layout/sidebar.tsx`. For sidebar requests, inspect the screenshot path and update the actual global sidebar list plus any module-specific sidebar helper.
2026-07-15 | Login page visual changes | User correction confirmed the existing VIMS login screen should not be redesigned into a split brand panel without explicit approval. For login UI changes, preserve the known centered logo and sign-in card unless the user asks for a redesign, and add a route-level assertion for the intended visible copy.
