2026-07-10 | Incident PDF witness output | A repeated user correction confirmed that current PDF output must not print legacy/stale fields simply because database columns contain values. For Witness Statement, only current UI-owned output belongs in the PDF: witness block heading, downloadable attachment link when present, and Remark. The old free-text `What the witness said` value must stay suppressed even for legacy populated rows.
2026-07-13 | Phase 1 payload builders | User correction exposed that new Phase 1 fields were added to the shared form but not to every route-level payload builder. When adding visible fields, update and test both create and edit route mappers, not just the form schema/backend serializer.
2026-07-13 | Incident rework target selection | User correction confirmed Incident Office Review rework must stay as one free-text instruction box, not a target-phase selector. When rework instructions need to be more visible, expose and render the saved phase-log comment instead of adding phase-picking UI.
2026-07-13 | Incident action simplification | User correction confirmed that removing status/open-close/verification controls from Phase 3/4 actions must not remove Due Date. When simplifying a form, classify each field separately as tracking/control versus recording data, and verify PDF/source-data requirements before hiding it.
2026-07-13 | Safety sidebar navigation | User correction showed that removing a Safety navigation item from `SafetySidebarGroup` is not enough when the visible app shell uses `components/layout/sidebar.tsx`. For sidebar requests, inspect the screenshot path and update the actual global sidebar list plus any module-specific sidebar helper.
2026-07-15 | Login page visual changes | User correction confirmed the existing VIMS login screen should not be redesigned into a split brand panel without explicit approval. For login UI changes, preserve the known centered logo and sign-in card unless the user asks for a redesign, and add a route-level assertion for the intended visible copy.
2026-08-17 | Audit permission status reporting | User correction confirmed that an SSOT-decided permission model must not be reported as open just because journey data is incomplete. Separate the decision, implementation wiring, and assigned-record test validation before reporting status.
2026-08-17 | Credential handling in handover docs | User correction confirmed test passwords must not be stored in Markdown reports or handover files. List personas/usernames only when needed and send passwords through the approved secret channel, with rotation after any plaintext exposure.
2026-08-17 | Audit handover status evidence | User correction confirmed Audit status answers must use the handover ledger when the question is about `Complete_VIMS_audit_dev`; do not infer those answers only from the current `Complete_VIMS` working tree.
2026-08-17 | Audit phase status wording | User correction confirmed phase status answers must state the direct yes/no result first. If a handover phase is closed but has release or later-tree caveats, say "closed" first and keep the caveat separate.
2026-08-17 | Audit journey verification evidence | Senior review exposed that a journey result is not formally verified unless the evidence package names the commit, account/persona, route, record IDs, command, raw output/log, and screenshot for manual checks. Treat partial local observations as unverified rather than marking them passed.
2026-08-18 | Audit UAT report packaging | Senior review confirmed narrative response Markdown is not enough for journey verification. Package each rerun as `UAT_REPORT_<date>.md` using the vendored UAT report contract, and include path:line evidence plus artifact hashes where artifacts are used.
2026-08-19 | Audit notification side effects | Audit plan create/edit must not let notification dispatch failures roll back the main plan action. When an SSOT says relay/channel failures are non-blocking, add a regression that forces the side effect to fail and proves the primary save still returns success.
2026-08-19 | Audit SQL Server UUID writes | SQL Server `uniqueidentifier` columns need explicit cast coverage on write paths, not only read/detail lookup paths. When fixing UUID conversion bugs, cover create, update, and reload SQL with tests that assert `CAST(... AS uniqueidentifier)`.
2026-08-19 | Audit demo seed enum drift | Audit journey/demo seed data must use the same SSOT enum values as the backend and frontend contracts. Do not seed shorthand values such as `OBS`, `IMPROVEMENT`, or `MAJOR`; use `OBSERVATION`, `IMPROVEMENT_SUGGESTION`, and `MAJOR_NC` so NC/Observation closure resolvers and journey routes exercise the real workflow instead of failing as missing records.
2026-08-20 | Audit SQL Server raw insert bounds | Raw SQL insert helpers must validate SQL Server character column bounds before executing. Django serializer/model max lengths do not protect manually built `INSERT` statements, so compare values against `INFORMATION_SCHEMA.COLUMNS` and return field-level validation errors instead of allowing SQL Server truncation 500s.
2026-08-20 | Audit registration child UUID inserts | Fixing only the parent `audit_detail` insert is incomplete when registration immediately writes child rows with `audit_detail_id` `uniqueidentifier` columns. Any SQL Server-safe Audit registration insert path must cover standards, team members, attendees, schedule blocks, and attachments instead of leaving those rows on ORM `bulk_create`.
2026-08-24 | Audit SQL Server UUID read joins | SQL Server `uniqueidentifier` conversion failures can also happen on read lookups when mssql-django builds ORM `id__in` filters from previously read UUID values. For shared master tables such as `mapping_role_user` and `master_role`, prefer the established raw SQL join pattern and cover the helper directly with a regression test.
2026-08-24 | Audit master UUID create recurrence | Qualified Auditor creation repeated the same SQL Server `uniqueidentifier` insert failure after earlier audit UUID fixes. New Audit master create APIs that write UUID primary keys must either cast explicit UUID values or omit the database-default column, and the regression test must exercise the SQL Server write branch, not only serializer validation.
2026-08-24 | Audit office-user role labels | When a dropdown is meant to prove an office user has a mapped `master_role`, do not fall back to unrelated fields such as `users.employee_role` or `department`. Filter unmapped users out so the UI does not imply they are configured for the workflow.
2026-08-24 | Circular duplicate active components | Before patching legacy Circular pages, trace the route import to the actual component. `/circular/office` renders `pages/circular/Officeuser.jsx`; patching the duplicate `Admin.jsx` does not affect that screen, so add route-source coverage when duplicates exist.
2026-08-25 | Audit lead auditor designation source | User correction confirmed Register Audit lead-auditor designation must show the actual mapped role/designation, not `users.employee_role` values such as Internal or Qualified Auditor scope values such as INTERNAL/EXTERNAL. Resolve person designations from the real role source (`mapping_role_user` to `master_role`) or leave the field blank instead of substituting workflow classification labels.
2026-08-25 | Audit registration plan UUID reads | Recurrence of the SQL Server UUID read pattern: Register Audit validation used `MasterAuditPlan.objects.get(id=plan_id)`, the consume path used `select_for_update().get(id=plan_id)`, and the duplicate-registration guard used ORM `exists()` on `audit_detail.audit_plan_id`, causing `uniqueidentifier` conversion failures before save. Any Audit route that accepts a plan/detail UUID must use SQL Server casted lookup/check helpers for validation, duplicate checks, and lock/consume paths, not only for detail-view routes.
2026-08-31 | Audit detail route UUID reads | Registered Audit detail still used `AuditDetail.objects.get(id=id)` from the URL and repeated the SQL Server `uniqueidentifier` conversion failure. Shared detail and scorecard routes must resolve URL UUIDs through a casted AuditDetail lookup helper before permission checks or bundle reads.
2026-09-01 | Audit finding save UUID reads | Save Finding repeated the Audit detail route UUID pattern in both the POST view lookup and the transactional creation lock. When fixing an Audit endpoint that starts from `{audit_id}`, inspect the whole request path for second-stage `select_for_update().get(id=...)` calls and cover both view and service with the same regression.
2026-09-01 | Audit finding clause UUID reads | Save Finding can pass audit-detail lookup and still fail while validating seeded clause UUIDs. Audit SQL Server fixes must cover nested master-table UUID checks such as `rule_clause_id` exists/get lookups, not only route IDs and child-table filters.
2026-09-01 | Audit finding row UUID inserts | Save Finding can pass UUID-safe reads and still fail when writing `audit_finding` or `audit_finding_clause` rows. SQL Server Audit finding creation must cast parent, checklist, circular, and clause UUID columns during inserts, and tests should cover parent and child rows together.
2026-09-01 | Audit finding response UUID reads | Save Finding can complete creation and still fail while serializing the response if serializers re-query child rows with ORM UUID filters. Audit response serializers that read child rows by `uniqueidentifier` parent IDs must use the same SQL Server-safe cast pattern as detail bundles and service helpers.
2026-09-01 | Audit NC closure UUID reads | Opening Audit NC closure can fail before any Dense/Wizard UI renders if the shared CAR workflow resolver or closure-record initializer uses ORM UUID filters on `audit_finding.id` or `audit_finding_nc.audit_finding_id`. Closure entry points must cast both the finding lookup and the one-to-one closure record lookup/create path in the same regression.
2026-09-01 | Audit finding action UUID reads | Observation closure and Issue Circular repeated the same SQL Server UUID pattern from NC closure after their view or service layers re-read `audit_finding`, `audit_detail`, or closure child rows. For every Audit finding action, cover the full click path with regression tests that fail on direct ORM UUID filters and assert the SQL Server `CAST(... AS uniqueidentifier)` helper is used for reads and link updates.
2026-09-02 | Safety SCM coordinate validation copy | User correction confirmed that replacing a generic validation message with another broad phrase is not enough. For coordinate fields, tell users the exact accepted format: decimal degrees with examples and minus signs for south/west instead of N/S/E/W letters.
2026-09-02 | Audit multipart upload copying | External audit registration crashed when `request.data.copy()` deep-copied a temporary uploaded PDF file handle. Multipart upload views must build serializer payloads from normal form fields and keep uploaded files in `request.FILES`; regression tests should force temporary-file upload handling, not only small in-memory uploads.
2026-09-02 | Audit external org UUID reads | External audit registration can still fail after upload handling if RO-delegation defaulting or explicit organisation validation uses ORM filters against SQL Server `uniqueidentifier` columns. Registration validation paths must cast vessel and organisation UUID lookups with the same raw-SQL pattern used for audit plans and detail rows.
2026-09-02 | Audit scorecard UUID writes | Save Scorecard repeated the SQL Server UUID pattern after read-side scorecard lookups were already fixed: creating a new `audit_area_summary` row through ORM can still send `audit_detail_id` as text to a `uniqueidentifier` column. When fixing an Audit endpoint, cover both existing-row update and missing-row create branches and assert write paths use the cast-aware insert helper.
2026-09-02 | Audit scorecard cleanup scope | User correction confirmed that scorecard cleanup means removing incorrect extra/demo data from the working grid and payload while preserving the established row styling. Do not redesign a working Audit UI presentation unless the user explicitly asks for a visual redesign.
2026-09-03 | Audit plan vessel list UUID reads | A vessel-scoped Audit plan list can still fail on SQL Server even after permission checks are fixed if `target_vessel_id` is filtered through the ORM. Dashboard/list endpoints that filter `uniqueidentifier` columns by the logged-in vessel must use the same `CAST(%s AS uniqueidentifier)` raw-SQL pattern as detail lookups.
